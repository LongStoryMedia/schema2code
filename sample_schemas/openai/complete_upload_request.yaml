$schema: http://json-schema.org/draft-07/schema#
title: CompleteUploadRequest
type: object
properties:
  md5:
    description: 'The optional md5 checksum for the file contents to verify if the
      bytes uploaded matches what you expect.

      '
    type: string
  part_ids:
    description: 'The ordered list of Part IDs.

      '
    items:
      type: string
    type: array
required:
- part_ids
additionalProperties: false
