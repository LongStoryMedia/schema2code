$schema: http://json-schema.org/draft-07/schema#
title: ChatCompletionFunctionCallOption
description: 'Specifying a particular function via `{"name": "my_function"}` forces
  the model to call that function.

  '
type: object
properties:
  name:
    description: The name of the function to call.
    type: string
required:
- name
