$schema: http://json-schema.org/draft-07/schema#
title: WebSearchContextSize
description: "High level guidance for the amount of context window space to use for\
  \ the \nsearch. One of `low`, `medium`, or `high`. `medium` is the default.\n"
type: string
enum:
- low
- medium
- high
default: medium
