$schema: http://json-schema.org/draft-07/schema#
title: FineTuneReinforcementMethod
description: Configuration for the reinforcement fine-tuning method.
type: object
properties:
  grader:
    anyOf:
    - $ref: grader_string_check.yaml
    - $ref: grader_text_similarity.yaml
    - $ref: grader_python.yaml
    - $ref: grader_score_model.yaml
    - $ref: grader_multi.yaml
    description: The grader used for the fine-tuning job.
    type: object
  hyperparameters:
    $ref: fine_tune_reinforcement_hyperparameters.yaml
required:
- grader
