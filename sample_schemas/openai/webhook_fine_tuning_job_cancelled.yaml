$schema: http://json-schema.org/draft-07/schema#
title: WebhookFineTuningJobCancelled
description: 'Sent when a fine-tuning job has been cancelled.

  '
type: object
properties:
  created_at:
    description: 'The Unix timestamp (in seconds) of when the fine-tuning job was
      cancelled.

      '
    type: integer
  data:
    description: 'Event data payload.

      '
    properties:
      id:
        description: 'The unique ID of the fine-tuning job.

          '
        type: string
    required:
    - id
    type: object
  id:
    description: 'The unique ID of the event.

      '
    type: string
  object:
    description: 'The object of the event. Always `event`.

      '
    enum:
    - event
    type: string
  type:
    description: 'The type of the event. Always `fine_tuning.job.cancelled`.

      '
    enum:
    - fine_tuning.job.cancelled
    type: string
required:
- created_at
- id
- data
- type
