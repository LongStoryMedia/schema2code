$schema: http://json-schema.org/draft-07/schema#
title: ModelIdsCompaction
description: Model ID used to generate the response, like `gpt-5` or `o3`. OpenAI
  offers a wide range of models with different capabilities, performance characteristics,
  and price points. Refer to the [model guide](https://platform.openai.com/docs/models)
  to browse and compare available models.
anyOf:
- $ref: model_ids_responses.yaml
- type: string
- type: 'null'
