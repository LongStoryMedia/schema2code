$schema: http://json-schema.org/draft-07/schema#
title: UserRoleUpdateRequest
type: object
properties:
  role:
    description: '`owner` or `reader`'
    enum:
    - owner
    - reader
    type: string
required:
- role
