// Code generated by schema2code at 2026-02-23 16:07:33. DO NOT EDIT.


import { FineTuningIntegration } from './FineTuningIntegration';

import { Metadata } from './Metadata';

import { FineTuneMethod } from './FineTuneMethod';



/**
 * The hyperparameters used for the fine-tuning job. This value will only be returned when running `supervised` jobs.
 */
export interface Hyperparameters {
  batch_size?: 'auto' | number | unknown;
  /**
   * Scaling factor for the learning rate. A smaller learning rate may be useful to avoid
overfitting.

   */
  learning_rate_multiplier?: 'auto' | number;
  /**
   * The number of epochs to train the model for. An epoch refers to one full cycle
through the training dataset.

   */
  n_epochs?: 'auto' | number;
}

/**
 * The `fine_tuning.job` object represents a fine-tuning job that has been created through the API.

 */
export interface FineTuningJob {
  /**
   * The Unix timestamp (in seconds) for when the fine-tuning job was created.
   */
  created_at: number;
  error: ErrorOption0 | unknown;
  estimated_finish?: number | unknown;
  fine_tuned_model: string | unknown;
  finished_at: number | unknown;
  /**
   * The hyperparameters used for the fine-tuning job. This value will only be returned when running `supervised` jobs.
   */
  hyperparameters: Hyperparameters;
  /**
   * The object identifier, which can be referenced in the API endpoints.
   */
  id: string;
  integrations?: (FineTuningIntegration)[] | unknown;
  metadata?: Metadata;
  method?: FineTuneMethod;
  /**
   * The base model that is being fine-tuned.
   */
  model: string;
  /**
   * The object type, which is always "fine_tuning.job".
   */
  object: 'fine_tuning.job';
  /**
   * The organization that owns the fine-tuning job.
   */
  organization_id: string;
  /**
   * The compiled results file ID(s) for the fine-tuning job. You can retrieve the results with the [Files API](https://platform.openai.com/docs/api-reference/files/retrieve-contents).
   */
  result_files: (string)[];
  /**
   * The seed used for the fine-tuning job.
   */
  seed: number;
  /**
   * The current status of the fine-tuning job, which can be either `validating_files`, `queued`, `running`, `succeeded`, `failed`, or `cancelled`.
   */
  status: 'validating_files' | 'queued' | 'running' | 'succeeded' | 'failed' | 'cancelled';
  trained_tokens: number | unknown;
  /**
   * The file ID used for training. You can retrieve the training data with the [Files API](https://platform.openai.com/docs/api-reference/files/retrieve-contents).
   */
  training_file: string;
  validation_file: string | unknown;
}