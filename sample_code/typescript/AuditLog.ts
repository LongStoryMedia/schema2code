// Code generated by schema2code at 2026-02-23 16:07:31. DO NOT EDIT.


import { AuditLogActor } from './AuditLogActor';

import { AuditLogEventType } from './AuditLogEventType';



/**
 * The details for events with this `type`.
 */
export interface ApiKeyCreated {
  /**
   * The payload used to create the API key.
   */
  data?: Data;
  /**
   * The tracking ID of the API key.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface ApiKeyDeleted {
  /**
   * The tracking ID of the API key.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface ApiKeyUpdated {
  /**
   * The payload used to update the API key.
   */
  changes_requested?: ChangesRequested;
  /**
   * The tracking ID of the API key.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface CertificateCreated {
  /**
   * The certificate ID.
   */
  id?: string;
  /**
   * The name of the certificate.
   */
  name?: string;
}

/**
 * The details for events with this `type`.
 */
export interface CertificateDeleted {
  /**
   * The certificate content in PEM format.
   */
  certificate?: string;
  /**
   * The certificate ID.
   */
  id?: string;
  /**
   * The name of the certificate.
   */
  name?: string;
}

/**
 * The details for events with this `type`.
 */
export interface CertificateUpdated {
  /**
   * The certificate ID.
   */
  id?: string;
  /**
   * The name of the certificate.
   */
  name?: string;
}

/**
 * The details for events with this `type`.
 */
export interface CertificatesActivated {
  certificates?: (CertificatesItem)[];
}

/**
 * The details for events with this `type`.
 */
export interface CertificatesDeactivated {
  certificates?: (CertificatesItem)[];
}

/**
 * The project and fine-tuned model checkpoint that the checkpoint permission was created for.
 */
export interface CheckpointPermissionCreated {
  /**
   * The payload used to create the checkpoint permission.
   */
  data?: Data;
  /**
   * The ID of the checkpoint permission.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface CheckpointPermissionDeleted {
  /**
   * The ID of the checkpoint permission.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface ExternalKeyRegistered {
  /**
   * The configuration for the external key.
   */
  data?: Record<string, unknown>;
  /**
   * The ID of the external key configuration.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface ExternalKeyRemoved {
  /**
   * The ID of the external key configuration.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface GroupCreated {
  /**
   * Information about the created group.
   */
  data?: Data;
  /**
   * The ID of the group.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface GroupDeleted {
  /**
   * The ID of the group.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface GroupUpdated {
  /**
   * The payload used to update the group.
   */
  changes_requested?: ChangesRequested;
  /**
   * The ID of the group.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface InviteAccepted {
  /**
   * The ID of the invite.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface InviteDeleted {
  /**
   * The ID of the invite.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface InviteSent {
  /**
   * The payload used to create the invite.
   */
  data?: Data;
  /**
   * The ID of the invite.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface IpAllowlistConfigActivated {
  /**
   * The configurations that were activated.
   */
  configs?: (ConfigsItem)[];
}

/**
 * The details for events with this `type`.
 */
export interface IpAllowlistConfigDeactivated {
  /**
   * The configurations that were deactivated.
   */
  configs?: (ConfigsItem)[];
}

/**
 * The details for events with this `type`.
 */
export interface IpAllowlistCreated {
  /**
   * The IP addresses or CIDR ranges included in the configuration.
   */
  allowed_ips?: (string)[];
  /**
   * The ID of the IP allowlist configuration.
   */
  id?: string;
  /**
   * The name of the IP allowlist configuration.
   */
  name?: string;
}

/**
 * The details for events with this `type`.
 */
export interface IpAllowlistDeleted {
  /**
   * The IP addresses or CIDR ranges that were in the configuration.
   */
  allowed_ips?: (string)[];
  /**
   * The ID of the IP allowlist configuration.
   */
  id?: string;
  /**
   * The name of the IP allowlist configuration.
   */
  name?: string;
}

/**
 * The details for events with this `type`.
 */
export interface IpAllowlistUpdated {
  /**
   * The updated set of IP addresses or CIDR ranges in the configuration.
   */
  allowed_ips?: (string)[];
  /**
   * The ID of the IP allowlist configuration.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface LoginFailed {
  /**
   * The error code of the failure.
   */
  error_code?: string;
  /**
   * The error message of the failure.
   */
  error_message?: string;
}

/**
 * The details for events with this `type`.
 */
export interface LogoutFailed {
  /**
   * The error code of the failure.
   */
  error_code?: string;
  /**
   * The error message of the failure.
   */
  error_message?: string;
}

/**
 * The details for events with this `type`.
 */
export interface OrganizationUpdated {
  /**
   * The payload used to update the organization settings.
   */
  changes_requested?: ChangesRequested;
  /**
   * The organization ID.
   */
  id?: string;
}

/**
 * The project that the action was scoped to. Absent for actions not scoped to projects. Note that any admin actions taken via Admin API keys are associated with the default project.
 */
export interface Project {
  /**
   * The project ID.
   */
  id?: string;
  /**
   * The project title.
   */
  name?: string;
}

/**
 * The details for events with this `type`.
 */
export interface ProjectArchived {
  /**
   * The project ID.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface ProjectCreated {
  /**
   * The payload used to create the project.
   */
  data?: Data;
  /**
   * The project ID.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface ProjectDeleted {
  /**
   * The project ID.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface ProjectUpdated {
  /**
   * The payload used to update the project.
   */
  changes_requested?: ChangesRequested;
  /**
   * The project ID.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface RateLimitDeleted {
  /**
   * The rate limit ID
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface RateLimitUpdated {
  /**
   * The payload used to update the rate limits.
   */
  changes_requested?: ChangesRequested;
  /**
   * The rate limit ID
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface RoleAssignmentCreated {
  /**
   * The identifier of the role assignment.
   */
  id?: string;
  /**
   * The principal (user or group) that received the role.
   */
  principal_id?: string;
  /**
   * The type of principal (user or group) that received the role.
   */
  principal_type?: string;
  /**
   * The resource the role assignment is scoped to.
   */
  resource_id?: string;
  /**
   * The type of resource the role assignment is scoped to.
   */
  resource_type?: string;
}

/**
 * The details for events with this `type`.
 */
export interface RoleAssignmentDeleted {
  /**
   * The identifier of the role assignment.
   */
  id?: string;
  /**
   * The principal (user or group) that had the role removed.
   */
  principal_id?: string;
  /**
   * The type of principal (user or group) that had the role removed.
   */
  principal_type?: string;
  /**
   * The resource the role assignment was scoped to.
   */
  resource_id?: string;
  /**
   * The type of resource the role assignment was scoped to.
   */
  resource_type?: string;
}

/**
 * The details for events with this `type`.
 */
export interface RoleCreated {
  /**
   * The role ID.
   */
  id?: string;
  /**
   * The permissions granted by the role.
   */
  permissions?: (string)[];
  /**
   * The resource the role is scoped to.
   */
  resource_id?: string;
  /**
   * The type of resource the role belongs to.
   */
  resource_type?: string;
  /**
   * The name of the role.
   */
  role_name?: string;
}

/**
 * The details for events with this `type`.
 */
export interface RoleDeleted {
  /**
   * The role ID.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface RoleUpdated {
  /**
   * The payload used to update the role.
   */
  changes_requested?: ChangesRequested;
  /**
   * The role ID.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface ScimDisabled {
  /**
   * The ID of the SCIM was disabled for.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface ScimEnabled {
  /**
   * The ID of the SCIM was enabled for.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface ServiceAccountCreated {
  /**
   * The payload used to create the service account.
   */
  data?: Data;
  /**
   * The service account ID.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface ServiceAccountDeleted {
  /**
   * The service account ID.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface ServiceAccountUpdated {
  /**
   * The payload used to updated the service account.
   */
  changes_requested?: ChangesRequested;
  /**
   * The service account ID.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface UserAdded {
  /**
   * The payload used to add the user to the project.
   */
  data?: Data;
  /**
   * The user ID.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface UserDeleted {
  /**
   * The user ID.
   */
  id?: string;
}

/**
 * The details for events with this `type`.
 */
export interface UserUpdated {
  /**
   * The payload used to update the user.
   */
  changes_requested?: ChangesRequested;
  /**
   * The project ID.
   */
  id?: string;
}

/**
 * A log of a user action or configuration change within this organization.
 */
export interface AuditLog {
  actor: AuditLogActor;
  /**
   * The details for events with this `type`.
   */
  api_key.created?: ApiKey.created;
  /**
   * The details for events with this `type`.
   */
  api_key.deleted?: ApiKey.deleted;
  /**
   * The details for events with this `type`.
   */
  api_key.updated?: ApiKey.updated;
  /**
   * The details for events with this `type`.
   */
  certificate.created?: Certificate.created;
  /**
   * The details for events with this `type`.
   */
  certificate.deleted?: Certificate.deleted;
  /**
   * The details for events with this `type`.
   */
  certificate.updated?: Certificate.updated;
  /**
   * The details for events with this `type`.
   */
  certificates.activated?: Certificates.activated;
  /**
   * The details for events with this `type`.
   */
  certificates.deactivated?: Certificates.deactivated;
  /**
   * The project and fine-tuned model checkpoint that the checkpoint permission was created for.
   */
  checkpoint.permission.created?: Checkpoint.permission.created;
  /**
   * The details for events with this `type`.
   */
  checkpoint.permission.deleted?: Checkpoint.permission.deleted;
  /**
   * The Unix timestamp (in seconds) of the event.
   */
  effective_at: number;
  /**
   * The details for events with this `type`.
   */
  external_key.registered?: ExternalKey.registered;
  /**
   * The details for events with this `type`.
   */
  external_key.removed?: ExternalKey.removed;
  /**
   * The details for events with this `type`.
   */
  group.created?: Group.created;
  /**
   * The details for events with this `type`.
   */
  group.deleted?: Group.deleted;
  /**
   * The details for events with this `type`.
   */
  group.updated?: Group.updated;
  /**
   * The ID of this log.
   */
  id: string;
  /**
   * The details for events with this `type`.
   */
  invite.accepted?: Invite.accepted;
  /**
   * The details for events with this `type`.
   */
  invite.deleted?: Invite.deleted;
  /**
   * The details for events with this `type`.
   */
  invite.sent?: Invite.sent;
  /**
   * The details for events with this `type`.
   */
  ip_allowlist.config.activated?: IpAllowlist.config.activated;
  /**
   * The details for events with this `type`.
   */
  ip_allowlist.config.deactivated?: IpAllowlist.config.deactivated;
  /**
   * The details for events with this `type`.
   */
  ip_allowlist.created?: IpAllowlist.created;
  /**
   * The details for events with this `type`.
   */
  ip_allowlist.deleted?: IpAllowlist.deleted;
  /**
   * The details for events with this `type`.
   */
  ip_allowlist.updated?: IpAllowlist.updated;
  /**
   * The details for events with this `type`.
   */
  login.failed?: Login.failed;
  /**
   * This event has no additional fields beyond the standard audit log attributes.
   */
  login.succeeded?: Record<string, unknown>;
  /**
   * The details for events with this `type`.
   */
  logout.failed?: Logout.failed;
  /**
   * This event has no additional fields beyond the standard audit log attributes.
   */
  logout.succeeded?: Record<string, unknown>;
  /**
   * The details for events with this `type`.
   */
  organization.updated?: Organization.updated;
  /**
   * The project that the action was scoped to. Absent for actions not scoped to projects. Note that any admin actions taken via Admin API keys are associated with the default project.
   */
  project?: Project;
  /**
   * The details for events with this `type`.
   */
  project.archived?: Project.archived;
  /**
   * The details for events with this `type`.
   */
  project.created?: Project.created;
  /**
   * The details for events with this `type`.
   */
  project.deleted?: Project.deleted;
  /**
   * The details for events with this `type`.
   */
  project.updated?: Project.updated;
  /**
   * The details for events with this `type`.
   */
  rate_limit.deleted?: RateLimit.deleted;
  /**
   * The details for events with this `type`.
   */
  rate_limit.updated?: RateLimit.updated;
  /**
   * The details for events with this `type`.
   */
  role.assignment.created?: Role.assignment.created;
  /**
   * The details for events with this `type`.
   */
  role.assignment.deleted?: Role.assignment.deleted;
  /**
   * The details for events with this `type`.
   */
  role.created?: Role.created;
  /**
   * The details for events with this `type`.
   */
  role.deleted?: Role.deleted;
  /**
   * The details for events with this `type`.
   */
  role.updated?: Role.updated;
  /**
   * The details for events with this `type`.
   */
  scim.disabled?: Scim.disabled;
  /**
   * The details for events with this `type`.
   */
  scim.enabled?: Scim.enabled;
  /**
   * The details for events with this `type`.
   */
  service_account.created?: ServiceAccount.created;
  /**
   * The details for events with this `type`.
   */
  service_account.deleted?: ServiceAccount.deleted;
  /**
   * The details for events with this `type`.
   */
  service_account.updated?: ServiceAccount.updated;
  type: AuditLogEventType;
  /**
   * The details for events with this `type`.
   */
  user.added?: User.added;
  /**
   * The details for events with this `type`.
   */
  user.deleted?: User.deleted;
  /**
   * The details for events with this `type`.
   */
  user.updated?: User.updated;
}