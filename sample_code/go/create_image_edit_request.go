// Code generated by schema2code at 2026-02-23 15:57:40. DO NOT EDIT.

package schema


// CreateImageEditRequestBackground Allows to set transparency for the background of the generated image(s).
This parameter is only supported for the GPT image models. Must be one of
`transparent`, `opaque` or `auto` (default value). When `auto` is used, the
model will automatically determine the best background for the image.

If `transparent`, the output format needs to support transparency, so it
should be set to either `png` (default value) or `webp`.

type CreateImageEditRequestBackground string

const (
	CreateImageEditRequestBackgroundTransparent CreateImageEditRequestBackground = "transparent"
	CreateImageEditRequestBackgroundOpaque CreateImageEditRequestBackground = "opaque"
	CreateImageEditRequestBackgroundAuto CreateImageEditRequestBackground = "auto"
)
// CreateImageEditRequestOutputFormat The format in which the generated images are returned. This parameter is
only supported for the GPT image models. Must be one of `png`, `jpeg`, or `webp`.
The default value is `png`.

type CreateImageEditRequestOutputFormat string

const (
	CreateImageEditRequestOutputFormatPng CreateImageEditRequestOutputFormat = "png"
	CreateImageEditRequestOutputFormatJpeg CreateImageEditRequestOutputFormat = "jpeg"
	CreateImageEditRequestOutputFormatWebp CreateImageEditRequestOutputFormat = "webp"
)
// CreateImageEditRequestQuality The quality of the image that will be generated. `high`, `medium` and `low` are only supported for the GPT image models. `dall-e-2` only supports `standard` quality. Defaults to `auto`.

type CreateImageEditRequestQuality string

const (
	CreateImageEditRequestQualityStandard CreateImageEditRequestQuality = "standard"
	CreateImageEditRequestQualityLow CreateImageEditRequestQuality = "low"
	CreateImageEditRequestQualityMedium CreateImageEditRequestQuality = "medium"
	CreateImageEditRequestQualityHigh CreateImageEditRequestQuality = "high"
	CreateImageEditRequestQualityAuto CreateImageEditRequestQuality = "auto"
)
// CreateImageEditRequestResponseFormat The format in which the generated images are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated. This parameter is only supported for `dall-e-2`, as the GPT image models always return base64-encoded images.
type CreateImageEditRequestResponseFormat string

const (
	CreateImageEditRequestResponseFormatUrl CreateImageEditRequestResponseFormat = "url"
	CreateImageEditRequestResponseFormatB64Json CreateImageEditRequestResponseFormat = "b64_json"
)
// CreateImageEditRequestSize The size of the generated images. Must be one of `1024x1024`, `1536x1024` (landscape), `1024x1536` (portrait), or `auto` (default value) for the GPT image models, and one of `256x256`, `512x512`, or `1024x1024` for `dall-e-2`.
type CreateImageEditRequestSize string

const (
	CreateImageEditRequestSize256x256 CreateImageEditRequestSize = "256x256"
	CreateImageEditRequestSize512x512 CreateImageEditRequestSize = "512x512"
	CreateImageEditRequestSize1024x1024 CreateImageEditRequestSize = "1024x1024"
	CreateImageEditRequestSize1536x1024 CreateImageEditRequestSize = "1536x1024"
	CreateImageEditRequestSize1024x1536 CreateImageEditRequestSize = "1024x1536"
	CreateImageEditRequestSizeAuto CreateImageEditRequestSize = "auto"
)
type CreateImageEditRequest struct {
	Background        *CreateImageEditRequestBackground `json:"background" yaml:"background" validate:"default="auto""` // Allows to set transparency for the background of the generated image(s).
This parameter is only supported for the GPT image models. Must be one of
`transparent`, `opaque` or `auto` (default value). When `auto` is used, the
model will automatically determine the best background for the image.

If `transparent`, the output format needs to support transparency, so it
should be set to either `png` (default value) or `webp`.

	Image             interface{}   `json:"image" yaml:"image" validate:"required"` // The image(s) to edit. Must be a supported image file or an array of images.

For the GPT image models (`gpt-image-1`, `gpt-image-1-mini`, and `gpt-image-1.5`), each image should be a `png`, `webp`, or `jpg`
file less than 50MB. You can provide up to 16 images.

For `dall-e-2`, you can only provide one image, and it should be a square
`png` file less than 4MB.

	InputFidelity     interface{}   `json:"input_fidelity" yaml:"input_fidelity"`
	Mask              *string       `json:"mask" yaml:"mask"` // An additional image whose fully transparent areas (e.g. where alpha is zero) indicate where `image` should be edited. If there are multiple images provided, the mask will be applied on the first image. Must be a valid PNG file, less than 4MB, and have the same dimensions as `image`.
	Model             interface{}   `json:"model" yaml:"model"` // The model to use for image generation. Only `dall-e-2` and the GPT image models are supported. Defaults to `dall-e-2` unless a parameter specific to the GPT image models is used.
	N                 *int          `json:"n" yaml:"n" validate:"min=1,max=10,default=1"` // The number of images to generate. Must be between 1 and 10.
	OutputCompression *int          `json:"output_compression" yaml:"output_compression" validate:"default=100"` // The compression level (0-100%) for the generated images. This parameter
is only supported for the GPT image models with the `webp` or `jpeg` output
formats, and defaults to 100.

	OutputFormat      *CreateImageEditRequestOutputFormat `json:"output_format" yaml:"output_format" validate:"default="png""` // The format in which the generated images are returned. This parameter is
only supported for the GPT image models. Must be one of `png`, `jpeg`, or `webp`.
The default value is `png`.

	PartialImages     *PartialImages `json:"partial_images" yaml:"partial_images"`
	Prompt            string        `json:"prompt" yaml:"prompt" validate:"required"` // A text description of the desired image(s). The maximum length is 1000 characters for `dall-e-2`, and 32000 characters for the GPT image models.
	Quality           *CreateImageEditRequestQuality `json:"quality" yaml:"quality" validate:"default="auto""` // The quality of the image that will be generated. `high`, `medium` and `low` are only supported for the GPT image models. `dall-e-2` only supports `standard` quality. Defaults to `auto`.

	ResponseFormat    *CreateImageEditRequestResponseFormat `json:"response_format" yaml:"response_format" validate:"default="url""` // The format in which the generated images are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated. This parameter is only supported for `dall-e-2`, as the GPT image models always return base64-encoded images.
	Size              *CreateImageEditRequestSize `json:"size" yaml:"size" validate:"default="1024x1024""` // The size of the generated images. Must be one of `1024x1024`, `1536x1024` (landscape), `1024x1536` (portrait), or `auto` (default value) for the GPT image models, and one of `256x256`, `512x512`, or `1024x1024` for `dall-e-2`.
	Stream            *bool         `json:"stream" yaml:"stream" validate:"default=False"` // Edit the image in streaming mode. Defaults to `false`. See the
[Image generation guide](https://platform.openai.com/docs/guides/image-generation) for more information.

	User              *string       `json:"user" yaml:"user"` // A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#end-user-ids).

}
