// Code generated by schema2code at 2026-02-23 15:57:41. DO NOT EDIT.

package schema



type CodeInterpreter struct {
	FileIds []string `json:"file_ids" yaml:"file_ids" validate:"default=[]"` // A list of [file](https://platform.openai.com/docs/api-reference/files) IDs made available to the `code_interpreter` tool. There can be a maximum of 20 files associated with the tool.

}

// AutoChunkingStrategyType Always `auto`.
type AutoChunkingStrategyType string

const (
	AutoChunkingStrategyTypeAuto AutoChunkingStrategyType = "auto"
)
// AutoChunkingStrategy The default strategy. This strategy currently uses a `max_chunk_size_tokens` of `800` and `chunk_overlap_tokens` of `400`.
type AutoChunkingStrategy struct {
	Type AutoChunkingStrategyType `json:"type" yaml:"type" validate:"required"` // Always `auto`.
}

type Static struct {
	ChunkOverlapTokens int `json:"chunk_overlap_tokens" yaml:"chunk_overlap_tokens" validate:"required"` // The number of tokens that overlap between chunks. The default value is `400`.

Note that the overlap must not exceed half of `max_chunk_size_tokens`.

	MaxChunkSizeTokens int `json:"max_chunk_size_tokens" yaml:"max_chunk_size_tokens" validate:"min=100,max=4096,required"` // The maximum number of tokens in each chunk. The default value is `800`. The minimum value is `100` and the maximum value is `4096`.
}

// StaticChunkingStrategyType Always `static`.
type StaticChunkingStrategyType string

const (
	StaticChunkingStrategyTypeStatic StaticChunkingStrategyType = "static"
)
type StaticChunkingStrategy struct {
	Static Static `json:"static" yaml:"static" validate:"required"`
	Type   StaticChunkingStrategyType `json:"type" yaml:"type" validate:"required"` // Always `static`.
}

type VectorStoresItem struct {
	ChunkingStrategy interface{} `json:"chunking_strategy" yaml:"chunking_strategy"` // The chunking strategy used to chunk the file(s). If not set, will use the `auto` strategy.
	FileIds          []string    `json:"file_ids" yaml:"file_ids"` // A list of [file](https://platform.openai.com/docs/api-reference/files) IDs to add to the vector store. There can be a maximum of 10000 files in a vector store.

	Metadata         *Metadata   `json:"metadata" yaml:"metadata"`
}

type FileSearch struct {
	VectorStoreIds []string           `json:"vector_store_ids" yaml:"vector_store_ids"` // The [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) attached to this thread. There can be a maximum of 1 vector store attached to the thread.

	VectorStores   []VectorStoresitem `json:"vector_stores" yaml:"vector_stores"` // A helper to create a [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) with file_ids and attach it to this thread. There can be a maximum of 1 vector store attached to the thread.

}

// ToolResources A set of resources that are made available to the assistant's tools in this thread. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs.

type ToolResources struct {
	CodeInterpreter *CodeInterpreter `json:"code_interpreter" yaml:"code_interpreter"`
	FileSearch      interface{}     `json:"file_search" yaml:"file_search"`
}

type CreateThreadAndRunRequest struct {
	AssistantID         string                            `json:"assistant_id" yaml:"assistant_id" validate:"required"` // The ID of the [assistant](https://platform.openai.com/docs/api-reference/assistants) to use to execute this run.
	Instructions        *string                           `json:"instructions" yaml:"instructions"` // Override the default system message of the assistant. This is useful for modifying the behavior on a per-run basis.
	MaxCompletionTokens *int                              `json:"max_completion_tokens" yaml:"max_completion_tokens" validate:"min=256"` // The maximum number of completion tokens that may be used over the course of the run. The run will make a best effort to use only the number of completion tokens specified, across multiple turns of the run. If the run exceeds the number of completion tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.

	MaxPromptTokens     *int                              `json:"max_prompt_tokens" yaml:"max_prompt_tokens" validate:"min=256"` // The maximum number of prompt tokens that may be used over the course of the run. The run will make a best effort to use only the number of prompt tokens specified, across multiple turns of the run. If the run exceeds the number of prompt tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.

	Metadata            *Metadata                         `json:"metadata" yaml:"metadata"`
	Model               interface{}                       `json:"model" yaml:"model"` // The ID of the [Model](https://platform.openai.com/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used.
	ParallelToolCalls   *ParallelToolCalls                `json:"parallel_tool_calls" yaml:"parallel_tool_calls"`
	ResponseFormat      *AssistantsApiResponseFormatOption `json:"response_format" yaml:"response_format"`
	Stream              *bool                             `json:"stream" yaml:"stream"` // If `true`, returns a stream of events that happen during the Run as server-sent events, terminating when the Run enters a terminal state with a `data: [DONE]` message.

	Temperature         *float32                          `json:"temperature" yaml:"temperature" validate:"min=0,max=2,default=1"` // What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.

	Thread              *CreateThreadRequest              `json:"thread" yaml:"thread"`
	ToolChoice          *string                           `json:"tool_choice" yaml:"tool_choice"`
	ToolResources       *ToolResources                    `json:"tool_resources" yaml:"tool_resources"` // A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs.

	Tools               []AssistantTool                   `json:"tools" yaml:"tools"` // Override the tools the assistant can use for this run. This is useful for modifying the behavior on a per-run basis.
	TopP                *float32                          `json:"top_p" yaml:"top_p" validate:"min=0,max=1,default=1"` // An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered.

We generally recommend altering this or temperature but not both.

	TruncationStrategy  *string                           `json:"truncation_strategy" yaml:"truncation_strategy"`
}
