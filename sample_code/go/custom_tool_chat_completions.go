// Code generated by schema2code at 2026-02-23 15:57:43. DO NOT EDIT.

package schema


// Custom Properties of the custom tool.

type Custom struct {
	Description *string     `json:"description" yaml:"description"` // Optional description of the custom tool, used to provide more context.

	Format      interface{} `json:"format" yaml:"format"` // The input format for the custom tool. Default is unconstrained text.

	Name        string      `json:"name" yaml:"name" validate:"required"` // The name of the custom tool, used to identify it in tool calls.
}

// TextformatType Unconstrained text format. Always `text`.
type TextformatType string

const (
	TextformatTypeText TextformatType = "text"
)
// Textformat Unconstrained free-form text.
type Textformat struct {
	Type TextformatType `json:"type" yaml:"type" validate:"required"` // Unconstrained text format. Always `text`.
}

// GrammarSyntax The syntax of the grammar definition. One of `lark` or `regex`.
type GrammarSyntax string

const (
	GrammarSyntaxLark GrammarSyntax = "lark"
	GrammarSyntaxRegex GrammarSyntax = "regex"
)
// Grammar Your chosen grammar.
type Grammar struct {
	Definition string `json:"definition" yaml:"definition" validate:"required"` // The grammar definition.
	Syntax     GrammarSyntax `json:"syntax" yaml:"syntax" validate:"required"` // The syntax of the grammar definition. One of `lark` or `regex`.
}

// GrammarformatType Grammar format. Always `grammar`.
type GrammarformatType string

const (
	GrammarformatTypeGrammar GrammarformatType = "grammar"
)
// Grammarformat A grammar defined by the user.
type Grammarformat struct {
	Grammar Grammar `json:"grammar" yaml:"grammar" validate:"required"` // Your chosen grammar.
	Type    GrammarformatType `json:"type" yaml:"type" validate:"required"` // Grammar format. Always `grammar`.
}

// CustomToolChatCompletionsType The type of the custom tool. Always `custom`.
type CustomToolChatCompletionsType string

const (
	CustomToolChatCompletionsTypeCustom CustomToolChatCompletionsType = "custom"
)
// CustomToolChatCompletions A custom tool that processes input using a specified format.

type CustomToolChatCompletions struct {
	Custom Custom `json:"custom" yaml:"custom" validate:"required"` // Properties of the custom tool.

	Type   CustomToolChatCompletionsType `json:"type" yaml:"type" validate:"required"` // The type of the custom tool. Always `custom`.
}
