// Code generated by schema2code at 2026-02-23 15:57:40. DO NOT EDIT.

package schema


// CreateImageRequestBackground Allows to set transparency for the background of the generated image(s).
This parameter is only supported for the GPT image models. Must be one of
`transparent`, `opaque` or `auto` (default value). When `auto` is used, the
model will automatically determine the best background for the image.

If `transparent`, the output format needs to support transparency, so it
should be set to either `png` (default value) or `webp`.

type CreateImageRequestBackground string

const (
	CreateImageRequestBackgroundTransparent CreateImageRequestBackground = "transparent"
	CreateImageRequestBackgroundOpaque CreateImageRequestBackground = "opaque"
	CreateImageRequestBackgroundAuto CreateImageRequestBackground = "auto"
)
// CreateImageRequestModeration Control the content-moderation level for images generated by the GPT image models. Must be either `low` for less restrictive filtering or `auto` (default value).
type CreateImageRequestModeration string

const (
	CreateImageRequestModerationLow CreateImageRequestModeration = "low"
	CreateImageRequestModerationAuto CreateImageRequestModeration = "auto"
)
// CreateImageRequestOutputFormat The format in which the generated images are returned. This parameter is only supported for the GPT image models. Must be one of `png`, `jpeg`, or `webp`.
type CreateImageRequestOutputFormat string

const (
	CreateImageRequestOutputFormatPng CreateImageRequestOutputFormat = "png"
	CreateImageRequestOutputFormatJpeg CreateImageRequestOutputFormat = "jpeg"
	CreateImageRequestOutputFormatWebp CreateImageRequestOutputFormat = "webp"
)
// CreateImageRequestQuality The quality of the image that will be generated.

- `auto` (default value) will automatically select the best quality for the given model.
- `high`, `medium` and `low` are supported for the GPT image models.
- `hd` and `standard` are supported for `dall-e-3`.
- `standard` is the only option for `dall-e-2`.

type CreateImageRequestQuality string

const (
	CreateImageRequestQualityStandard CreateImageRequestQuality = "standard"
	CreateImageRequestQualityHd CreateImageRequestQuality = "hd"
	CreateImageRequestQualityLow CreateImageRequestQuality = "low"
	CreateImageRequestQualityMedium CreateImageRequestQuality = "medium"
	CreateImageRequestQualityHigh CreateImageRequestQuality = "high"
	CreateImageRequestQualityAuto CreateImageRequestQuality = "auto"
)
// CreateImageRequestResponseFormat The format in which generated images with `dall-e-2` and `dall-e-3` are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated. This parameter isn't supported for the GPT image models, which always return base64-encoded images.
type CreateImageRequestResponseFormat string

const (
	CreateImageRequestResponseFormatUrl CreateImageRequestResponseFormat = "url"
	CreateImageRequestResponseFormatB64Json CreateImageRequestResponseFormat = "b64_json"
)
// CreateImageRequestSize The size of the generated images. Must be one of `1024x1024`, `1536x1024` (landscape), `1024x1536` (portrait), or `auto` (default value) for the GPT image models, one of `256x256`, `512x512`, or `1024x1024` for `dall-e-2`, and one of `1024x1024`, `1792x1024`, or `1024x1792` for `dall-e-3`.
type CreateImageRequestSize string

const (
	CreateImageRequestSizeAuto CreateImageRequestSize = "auto"
	CreateImageRequestSize1024x1024 CreateImageRequestSize = "1024x1024"
	CreateImageRequestSize1536x1024 CreateImageRequestSize = "1536x1024"
	CreateImageRequestSize1024x1536 CreateImageRequestSize = "1024x1536"
	CreateImageRequestSize256x256 CreateImageRequestSize = "256x256"
	CreateImageRequestSize512x512 CreateImageRequestSize = "512x512"
	CreateImageRequestSize1792x1024 CreateImageRequestSize = "1792x1024"
	CreateImageRequestSize1024x1792 CreateImageRequestSize = "1024x1792"
)
// CreateImageRequestStyle The style of the generated images. This parameter is only supported for `dall-e-3`. Must be one of `vivid` or `natural`. Vivid causes the model to lean towards generating hyper-real and dramatic images. Natural causes the model to produce more natural, less hyper-real looking images.
type CreateImageRequestStyle string

const (
	CreateImageRequestStyleVivid CreateImageRequestStyle = "vivid"
	CreateImageRequestStyleNatural CreateImageRequestStyle = "natural"
)
type CreateImageRequest struct {
	Background        *CreateImageRequestBackground `json:"background" yaml:"background" validate:"default="auto""` // Allows to set transparency for the background of the generated image(s).
This parameter is only supported for the GPT image models. Must be one of
`transparent`, `opaque` or `auto` (default value). When `auto` is used, the
model will automatically determine the best background for the image.

If `transparent`, the output format needs to support transparency, so it
should be set to either `png` (default value) or `webp`.

	Model             interface{}   `json:"model" yaml:"model"` // The model to use for image generation. One of `dall-e-2`, `dall-e-3`, or a GPT image model (`gpt-image-1`, `gpt-image-1-mini`, `gpt-image-1.5`). Defaults to `dall-e-2` unless a parameter specific to the GPT image models is used.
	Moderation        *CreateImageRequestModeration `json:"moderation" yaml:"moderation" validate:"default="auto""` // Control the content-moderation level for images generated by the GPT image models. Must be either `low` for less restrictive filtering or `auto` (default value).
	N                 *int          `json:"n" yaml:"n" validate:"min=1,max=10,default=1"` // The number of images to generate. Must be between 1 and 10. For `dall-e-3`, only `n=1` is supported.
	OutputCompression *int          `json:"output_compression" yaml:"output_compression" validate:"default=100"` // The compression level (0-100%) for the generated images. This parameter is only supported for the GPT image models with the `webp` or `jpeg` output formats, and defaults to 100.
	OutputFormat      *CreateImageRequestOutputFormat `json:"output_format" yaml:"output_format" validate:"default="png""` // The format in which the generated images are returned. This parameter is only supported for the GPT image models. Must be one of `png`, `jpeg`, or `webp`.
	PartialImages     *PartialImages `json:"partial_images" yaml:"partial_images"`
	Prompt            string        `json:"prompt" yaml:"prompt" validate:"required"` // A text description of the desired image(s). The maximum length is 32000 characters for the GPT image models, 1000 characters for `dall-e-2` and 4000 characters for `dall-e-3`.
	Quality           *CreateImageRequestQuality `json:"quality" yaml:"quality" validate:"default="auto""` // The quality of the image that will be generated.

- `auto` (default value) will automatically select the best quality for the given model.
- `high`, `medium` and `low` are supported for the GPT image models.
- `hd` and `standard` are supported for `dall-e-3`.
- `standard` is the only option for `dall-e-2`.

	ResponseFormat    *CreateImageRequestResponseFormat `json:"response_format" yaml:"response_format" validate:"default="url""` // The format in which generated images with `dall-e-2` and `dall-e-3` are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated. This parameter isn't supported for the GPT image models, which always return base64-encoded images.
	Size              *CreateImageRequestSize `json:"size" yaml:"size" validate:"default="auto""` // The size of the generated images. Must be one of `1024x1024`, `1536x1024` (landscape), `1024x1536` (portrait), or `auto` (default value) for the GPT image models, one of `256x256`, `512x512`, or `1024x1024` for `dall-e-2`, and one of `1024x1024`, `1792x1024`, or `1024x1792` for `dall-e-3`.
	Stream            *bool         `json:"stream" yaml:"stream" validate:"default=False"` // Generate the image in streaming mode. Defaults to `false`. See the
[Image generation guide](https://platform.openai.com/docs/guides/image-generation) for more information.
This parameter is only supported for the GPT image models.

	Style             *CreateImageRequestStyle `json:"style" yaml:"style" validate:"default="vivid""` // The style of the generated images. This parameter is only supported for `dall-e-3`. Must be one of `vivid` or `natural`. Vivid causes the model to lean towards generating hyper-real and dramatic images. Natural causes the model to produce more natural, less hyper-real looking images.
	User              *string       `json:"user" yaml:"user"` // A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#end-user-ids).

}
