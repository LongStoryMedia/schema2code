// Code generated by schema2code at 2026-02-23 15:58:24. DO NOT EDIT.

package schema


// Audio Configuration for input and output audio.

type Audio struct {
	Input  *Input `json:"input" yaml:"input"`
	Output *Output `json:"output" yaml:"output"`
}

// NoiseReduction Configuration for input audio noise reduction. This can be set to `null` to turn off.
Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.

type NoiseReduction struct {
	Type *NoiseReductionType `json:"type" yaml:"type"`
}

type Input struct {
	Format         *RealtimeAudioFormats `json:"format" yaml:"format"` // The format of the input audio.
	NoiseReduction *NoiseReduction       `json:"noise_reduction" yaml:"noise_reduction"` // Configuration for input audio noise reduction. This can be set to `null` to turn off.
Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.

	Transcription  *AudioTranscription   `json:"transcription" yaml:"transcription"` // Configuration for input audio transcription, defaults to off and can be set to `null` to turn off once on. Input audio transcription is not native to the model, since the model consumes audio directly. Transcription runs asynchronously through [the /audio/transcriptions endpoint](https://platform.openai.com/docs/api-reference/audio/createTranscription) and should be treated as guidance of input audio content rather than precisely what the model heard. The client can optionally set the language and prompt for transcription, these offer additional guidance to the transcription service.

	TurnDetection  *RealtimeTurnDetection `json:"turn_detection" yaml:"turn_detection"`
}

type Output struct {
	Format *RealtimeAudioFormats `json:"format" yaml:"format"` // The format of the output audio.
	Speed  *float32             `json:"speed" yaml:"speed" validate:"min=0.25,max=1.5,default=1"` // The speed of the model's spoken response as a multiple of the original speed.
1.0 is the default speed. 0.25 is the minimum speed. 1.5 is the maximum speed. This value can only be changed in between model turns, not while a response is in progress.

This parameter is a post-processing adjustment to the audio after it is generated, it's
also possible to prompt the model to speak faster or slower.

	Voice  *VoiceIdsShared      `json:"voice" yaml:"voice" validate:"default="alloy""` // The voice the model uses to respond. Voice cannot be changed during the
session once the model has responded with audio at least once. Current
voice options are `alloy`, `ash`, `ballad`, `coral`, `echo`, `sage`,
`shimmer`, `verse`, `marin`, and `cedar`. We recommend `marin` and `cedar` for
best quality.

}

// ClientSecret Ephemeral key returned by the API.
type ClientSecret struct {
	ExpiresAt int    `json:"expires_at" yaml:"expires_at" validate:"required"` // Timestamp for when the token expires. Currently, all tokens expire
after one minute.

	Value     string `json:"value" yaml:"value" validate:"required"` // Ephemeral key usable in client environments to authenticate connections to the Realtime API. Use this in client-side environments rather than a standard API token, which should only be used server-side.

}

// RealtimeSessionCreateResponseGAType The type of session to create. Always `realtime` for the Realtime API.

type RealtimeSessionCreateResponseGAType string

const (
	RealtimeSessionCreateResponseGATypeRealtime RealtimeSessionCreateResponseGAType = "realtime"
)
// RealtimeSessionCreateResponseGA A new Realtime session configuration, with an ephemeral key. Default TTL
for keys is one minute.

type RealtimeSessionCreateResponseGA struct {
	Audio            *Audio             `json:"audio" yaml:"audio"` // Configuration for input and output audio.

	ClientSecret     ClientSecret       `json:"client_secret" yaml:"client_secret" validate:"required"` // Ephemeral key returned by the API.
	Include          []string           `json:"include" yaml:"include"` // Additional fields to include in server outputs.

`item.input_audio_transcription.logprobs`: Include logprobs for input audio transcription.

	Instructions     *string            `json:"instructions" yaml:"instructions"` // The default system instructions (i.e. system message) prepended to model calls. This field allows the client to guide the model on desired responses. The model can be instructed on response content and format, (e.g. "be extremely succinct", "act friendly", "here are examples of good responses") and on audio behavior (e.g. "talk quickly", "inject emotion into your voice", "laugh frequently"). The instructions are not guaranteed to be followed by the model, but they provide guidance to the model on the desired behavior.

Note that the server sets default instructions which will be used if this field is not set and are visible in the `session.created` event at the start of the session.

	MaxOutputTokens  interface{}        `json:"max_output_tokens" yaml:"max_output_tokens"` // Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.

	Model            interface{}        `json:"model" yaml:"model"` // The Realtime model used for this session.

	OutputModalities []string           `json:"output_modalities" yaml:"output_modalities" validate:"default=['audio']"` // The set of modalities the model can respond with. It defaults to `["audio"]`, indicating
that the model will respond with audio plus a transcript. `["text"]` can be used to make
the model respond with text only. It is not possible to request both `text` and `audio` at the same time.

	Prompt           *Prompt            `json:"prompt" yaml:"prompt"`
	ToolChoice       interface{}        `json:"tool_choice" yaml:"tool_choice" validate:"default="auto""` // How the model chooses tools. Provide one of the string modes or force a specific
function/MCP tool.

	Tools            []interface{}      `json:"tools" yaml:"tools"` // Tools available to the model.
	Tracing          interface{}        `json:"tracing" yaml:"tracing"`
	Truncation       *RealtimeTruncation `json:"truncation" yaml:"truncation"`
	Type             RealtimeSessionCreateResponseGAType `json:"type" yaml:"type" validate:"required"` // The type of session to create. Always `realtime` for the Realtime API.

}
