// Code generated by schema2code at 2026-02-23 15:58:24. DO NOT EDIT.

package schema


// ClientSecret Ephemeral key returned by the API.
type ClientSecret struct {
	ExpiresAt int    `json:"expires_at" yaml:"expires_at" validate:"required"` // Timestamp for when the token expires. Currently, all tokens expire
after one minute.

	Value     string `json:"value" yaml:"value" validate:"required"` // Ephemeral key usable in client environments to authenticate connections
to the Realtime API. Use this in client-side environments rather than
a standard API token, which should only be used server-side.

}

// InputAudioTranscription Configuration for input audio transcription, defaults to off and can be
set to `null` to turn off once on. Input audio transcription is not native
to the model, since the model consumes audio directly. Transcription runs
asynchronously and should be treated as rough guidance
rather than the representation understood by the model.

type InputAudioTranscription struct {
	Model *string `json:"model" yaml:"model"` // The model to use for transcription.

}

// ToolsItemType The type of the tool, i.e. `function`.
type ToolsItemType string

const (
	ToolsItemTypeFunction ToolsItemType = "function"
)
type ToolsItem struct {
	Description *string                `json:"description" yaml:"description"` // The description of the function, including guidance on when and how
to call it, and guidance about what to tell the user when calling
(if anything).

	Name        *string                `json:"name" yaml:"name"` // The name of the function.
	Parameters  map[string]interface{} `json:"parameters" yaml:"parameters"` // Parameters of the function in JSON Schema.
	Type        *ToolsItemType         `json:"type" yaml:"type"` // The type of the tool, i.e. `function`.
}

// TracingConfiguration Granular configuration for tracing.

type TracingConfiguration struct {
	GroupID      *string                `json:"group_id" yaml:"group_id"` // The group id to attach to this trace to enable filtering and
grouping in the traces dashboard.

	Metadata     map[string]interface{} `json:"metadata" yaml:"metadata"` // The arbitrary metadata to attach to this trace to enable
filtering in the traces dashboard.

	WorkflowName *string                `json:"workflow_name" yaml:"workflow_name"` // The name of the workflow to attach to this trace. This is used to
name the trace in the traces dashboard.

}

// TurnDetection Configuration for turn detection. Can be set to `null` to turn off. Server
VAD means that the model will detect the start and end of speech based on
audio volume and respond at the end of user speech.

type TurnDetection struct {
	PrefixPaddingMs   *int    `json:"prefix_padding_ms" yaml:"prefix_padding_ms"` // Amount of audio to include before the VAD detected speech (in
milliseconds). Defaults to 300ms.

	SilenceDurationMs *int    `json:"silence_duration_ms" yaml:"silence_duration_ms"` // Duration of silence to detect speech stop (in milliseconds). Defaults
to 500ms. With shorter values the model will respond more quickly,
but may jump in on short pauses from the user.

	Threshold         *float32 `json:"threshold" yaml:"threshold"` // Activation threshold for VAD (0.0 to 1.0), this defaults to 0.5. A
higher threshold will require louder audio to activate the model, and
thus might perform better in noisy environments.

	Type              *string `json:"type" yaml:"type"` // Type of turn detection, only `server_vad` is currently supported.

}

// RealtimeSessionCreateRequest A new Realtime session configuration, with an ephemeral key. Default TTL
for keys is one minute.

type RealtimeSessionCreateRequest struct {
	ClientSecret            ClientSecret            `json:"client_secret" yaml:"client_secret" validate:"required"` // Ephemeral key returned by the API.
	InputAudioFormat        *string                 `json:"input_audio_format" yaml:"input_audio_format"` // The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.

	InputAudioTranscription *InputAudioTranscription `json:"input_audio_transcription" yaml:"input_audio_transcription"` // Configuration for input audio transcription, defaults to off and can be
set to `null` to turn off once on. Input audio transcription is not native
to the model, since the model consumes audio directly. Transcription runs
asynchronously and should be treated as rough guidance
rather than the representation understood by the model.

	Instructions            *string                 `json:"instructions" yaml:"instructions"` // The default system instructions (i.e. system message) prepended to model calls. This field allows the client to guide the model on desired responses. The model can be instructed on response content and format, (e.g. "be extremely succinct", "act friendly", "here are examples of good responses") and on audio behavior (e.g. "talk quickly", "inject emotion into your voice", "laugh frequently"). The instructions are not guaranteed to be followed by the model, but they provide guidance to the model on the desired behavior.
Note that the server sets default instructions which will be used if this field is not set and are visible in the `session.created` event at the start of the session.

	MaxResponseOutputTokens interface{}             `json:"max_response_output_tokens" yaml:"max_response_output_tokens"` // Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.

	Modalities              *string                 `json:"modalities" yaml:"modalities"` // The set of modalities the model can respond with. To disable audio,
set this to ["text"].

	OutputAudioFormat       *string                 `json:"output_audio_format" yaml:"output_audio_format"` // The format of output audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.

	Prompt                  *Prompt                 `json:"prompt" yaml:"prompt"`
	Speed                   *float32                `json:"speed" yaml:"speed" validate:"min=0.25,max=1.5,default=1"` // The speed of the model's spoken response. 1.0 is the default speed. 0.25 is
the minimum speed. 1.5 is the maximum speed. This value can only be changed
in between model turns, not while a response is in progress.

	Temperature             *float32                `json:"temperature" yaml:"temperature"` // Sampling temperature for the model, limited to [0.6, 1.2]. Defaults to 0.8.

	ToolChoice              *string                 `json:"tool_choice" yaml:"tool_choice"` // How the model chooses tools. Options are `auto`, `none`, `required`, or
specify a function.

	Tools                   []Toolsitem             `json:"tools" yaml:"tools"` // Tools (functions) available to the model.
	Tracing                 interface{}             `json:"tracing" yaml:"tracing"` // Configuration options for tracing. Set to null to disable tracing. Once
tracing is enabled for a session, the configuration cannot be modified.

`auto` will create a trace for the session with default values for the
workflow name, group id, and metadata.

	Truncation              *RealtimeTruncation     `json:"truncation" yaml:"truncation"`
	TurnDetection           *TurnDetection          `json:"turn_detection" yaml:"turn_detection"` // Configuration for turn detection. Can be set to `null` to turn off. Server
VAD means that the model will detect the start and end of speech based on
audio volume and respond at the end of user speech.

	Voice                   *VoiceIdsShared         `json:"voice" yaml:"voice"` // The voice the model uses to respond. Supported built-in voices are `alloy`, `ash`, `ballad`, `coral`, `echo`, `sage`, `shimmer`, `verse`, `marin`, and `cedar`. Voice cannot be changed during the session once the model has responded with audio at least once.
}
