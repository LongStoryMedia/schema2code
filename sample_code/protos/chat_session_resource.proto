// GENERATED CODE - DO NOT EDIT
// Generated by schema2code proto generator

syntax = "proto3";

package schema;

import "chat_session_chatkit_configuration.proto";
import "chat_session_rate_limits.proto";
import "chat_session_status.proto";
import "chatkit_workflow.proto";

// Represents a ChatKit session and its resolved configuration.
message ChatSessionResource {
  // Resolved ChatKit feature configuration for the session.
  ChatSessionChatkitConfiguration chatkit_configuration = 1;
  // Ephemeral client secret that authenticates session requests.
  string client_secret = 2;
  // Unix timestamp (in seconds) for when the session expires.
  int32 expires_at = 3;
  // Identifier for the ChatKit session.
  string id = 4;
  // Convenience copy of the per-minute request limit.
  int32 max_requests_per_1_minute = 5;
  // Type discriminator that is always `chatkit.session`.
  enum ObjectEnum {
    OBJECTENUM_UNSPECIFIED = 0;
    CHATKIT_SESSION = 1;
  }
  

  // Type discriminator that is always `chatkit.session`.
  ObjectEnum object = 6;
  // Resolved rate limit values.
  ChatSessionRateLimits rate_limits = 7;
  // Current lifecycle state of the session.
  ChatSessionStatus status = 8;
  // User identifier associated with the session.
  string user = 9;
  // Workflow metadata for the session.
  ChatkitWorkflow workflow = 10;
}
