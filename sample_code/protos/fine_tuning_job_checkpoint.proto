// GENERATED CODE - DO NOT EDIT
// Generated by schema2code proto generator

syntax = "proto3";

package schema;

// The `fine_tuning.job.checkpoint` object represents a model checkpoint for a fine-tuning job that is ready to use.
message FineTuningJobCheckpoint {
  // The Unix timestamp (in seconds) for when the checkpoint was created.
  int32 created_at = 1;
  // The name of the fine-tuned checkpoint model that is created.
  string fine_tuned_model_checkpoint = 2;
  // The name of the fine-tuning job that this checkpoint was created from.
  string fine_tuning_job_id = 3;
  // The checkpoint identifier, which can be referenced in the API endpoints.
  string id = 4;
  // Metrics at the step number during the fine-tuning job.
  message Metrics {
  message Metrics {
    double full_valid_loss = 1;
    double full_valid_mean_token_accuracy = 2;
    double step = 3;
    double train_loss = 4;
    double train_mean_token_accuracy = 5;
    double valid_loss = 6;
    double valid_mean_token_accuracy = 7;
  }
  

  // Metrics at the step number during the fine-tuning job.
  Metrics metrics = 5;
  // The object type, which is always "fine_tuning.job.checkpoint".
  enum ObjectEnum {
    OBJECTENUM_UNSPECIFIED = 0;
    FINE_TUNING_JOB_CHECKPOINT = 1;
  }
  

  // The object type, which is always "fine_tuning.job.checkpoint".
  ObjectEnum object = 6;
  // The step number that the checkpoint was created at.
  int32 step_number = 7;
}
