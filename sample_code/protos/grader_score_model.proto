// GENERATED CODE - DO NOT EDIT
// Generated by schema2code proto generator

syntax = "proto3";

package schema;

import "eval_item.proto";

// A ScoreModelGrader object that uses a model to assign a score to the input.
message GraderScoreModel {
  // The input messages evaluated by the grader. Supports text, output text, input image, and input audio content blocks, and may include template strings.
  repeated EvalItem input = 1;
  // The model to use for the evaluation.
  string model = 2;
  // The name of the grader.
  string name = 3;
  // The range of the score. Defaults to `[0, 1]`.
  repeated double range = 4;
  // The sampling parameters for the model.
  message SamplingParams {
  message SamplingParams {
    int32 max_completions_tokens = 1;
    ReasoningEffort reasoning_effort = 2;
    int32 seed = 3;
    double temperature = 4;
    double top_p = 5;
  }
  

  // The sampling parameters for the model.
  SamplingParams sampling_params = 5;
  // The object type, which is always `score_model`.
  enum TypeEnum {
    TYPEENUM_UNSPECIFIED = 0;
    SCORE_MODEL = 1;
  }
  

  // The object type, which is always `score_model`.
  TypeEnum type = 6;
}
