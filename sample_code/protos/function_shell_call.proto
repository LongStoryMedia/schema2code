// GENERATED CODE - DO NOT EDIT
// Generated by schema2code proto generator

syntax = "proto3";

package schema;

import "function_shell_action.proto";
import "local_shell_call_status.proto";

// A tool call that executes one or more shell commands in a managed environment.
message FunctionShellCall {
  // The shell commands and limits that describe how to run the tool call.
  FunctionShellAction action = 1;
  // The unique ID of the shell tool call generated by the model.
  string call_id = 2;
  // The ID of the entity that created this tool call.
  string created_by = 3;
  // The unique ID of the shell tool call. Populated when this item is returned via API.
  string id = 4;
  // The status of the shell call. One of `in_progress`, `completed`, or `incomplete`.
  LocalShellCallStatus status = 5;
  // The type of the item. Always `shell_call`.
  enum TypeEnum {
    TYPEENUM_UNSPECIFIED = 0;
    SHELL_CALL = 1;
  }
  

  // The type of the item. Always `shell_call`.
  TypeEnum type = 6;
}
