// GENERATED CODE - DO NOT EDIT
// Generated by schema2code proto generator

syntax = "proto3";

package schema;

import "computer_action.proto";
import "computer_call_safety_check_param.proto";

// A tool call to a computer use tool. See the
[computer use guide](https://platform.openai.com/docs/guides/tools-computer-use) for more information.
message ComputerToolCall {
  ComputerAction action = 1;
  // An identifier used when responding to the tool call with output.
  string call_id = 2;
  // The unique ID of the computer call.
  string id = 3;
  // The pending safety checks for the computer call.
  repeated ComputerCallSafetyCheckParam pending_safety_checks = 4;
  // The status of the item. One of `in_progress`, `completed`, or
`incomplete`. Populated when items are returned via API.
  enum StatusEnum {
    STATUSENUM_UNSPECIFIED = 0;
    IN_PROGRESS = 1;
    COMPLETED = 2;
    INCOMPLETE = 3;
  }
  

  // The status of the item. One of `in_progress`, `completed`, or
`incomplete`. Populated when items are returned via API.
  StatusEnum status = 5;
  // The type of the computer call. Always `computer_call`.
  enum TypeEnum {
    TYPEENUM_UNSPECIFIED = 0;
    COMPUTER_CALL = 1;
  }
  

  // The type of the computer call. Always `computer_call`.
  TypeEnum type = 6;
}
