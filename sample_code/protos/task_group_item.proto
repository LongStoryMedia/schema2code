// GENERATED CODE - DO NOT EDIT
// Generated by schema2code proto generator

syntax = "proto3";

package schema;

import "task_group_task.proto";

// Collection of workflow tasks grouped together in the thread.
message TaskGroupItem {
  // Unix timestamp (in seconds) for when the item was created.
  int32 created_at = 1;
  // Identifier of the thread item.
  string id = 2;
  // Type discriminator that is always `chatkit.thread_item`.
  enum ObjectEnum {
    OBJECTENUM_UNSPECIFIED = 0;
    CHATKIT_THREAD_ITEM = 1;
  }
  

  // Type discriminator that is always `chatkit.thread_item`.
  ObjectEnum object = 3;
  // Tasks included in the group.
  repeated TaskGroupTask tasks = 4;
  // Identifier of the parent thread.
  string thread_id = 5;
  // Type discriminator that is always `chatkit.task_group`.
  enum TypeEnum {
    TYPEENUM_UNSPECIFIED = 0;
    CHATKIT_TASK_GROUP = 1;
  }
  

  // Type discriminator that is always `chatkit.task_group`.
  TypeEnum type = 6;
}
