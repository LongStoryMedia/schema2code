// GENERATED CODE - DO NOT EDIT
// Generated by schema2code proto generator

syntax = "proto3";

package schema;

// A tool that generates images using the GPT image models.
message ImageGenTool {
  // Background type for the generated image. One of `transparent`,
`opaque`, or `auto`. Default: `auto`.
  enum BackgroundEnum {
    BACKGROUNDENUM_UNSPECIFIED = 0;
    TRANSPARENT = 1;
    OPAQUE = 2;
    AUTO = 3;
  }
  

  // Background type for the generated image. One of `transparent`,
`opaque`, or `auto`. Default: `auto`.
  BackgroundEnum background = 1;
  InputFidelity input_fidelity = 2;
  // Optional mask for inpainting. Contains `image_url`
(string, optional) and `file_id` (string, optional).

  message InputImageMask {
  message InputImageMask {
    // File ID for the mask image.
    string file_id = 1;
    // Base64-encoded mask image.
    string image_url = 2;
  }
  

  // Optional mask for inpainting. Contains `image_url`
(string, optional) and `file_id` (string, optional).
  InputImageMask input_image_mask = 3;
  string model = 4;
  // Moderation level for the generated image. Default: `auto`.
  enum ModerationEnum {
    MODERATIONENUM_UNSPECIFIED = 0;
    AUTO = 1;
    LOW = 2;
  }
  

  // Moderation level for the generated image. Default: `auto`.
  ModerationEnum moderation = 5;
  // Compression level for the output image. Default: 100.
  int32 output_compression = 6;
  // The output format of the generated image. One of `png`, `webp`, or
`jpeg`. Default: `png`.
  enum OutputFormatEnum {
    OUTPUTFORMATENUM_UNSPECIFIED = 0;
    PNG = 1;
    WEBP = 2;
    JPEG = 3;
  }
  

  // The output format of the generated image. One of `png`, `webp`, or
`jpeg`. Default: `png`.
  OutputFormatEnum output_format = 7;
  // Number of partial images to generate in streaming mode, from 0 (default value) to 3.
  int32 partial_images = 8;
  // The quality of the generated image. One of `low`, `medium`, `high`,
or `auto`. Default: `auto`.
  enum QualityEnum {
    QUALITYENUM_UNSPECIFIED = 0;
    LOW = 1;
    MEDIUM = 2;
    HIGH = 3;
    AUTO = 4;
  }
  

  // The quality of the generated image. One of `low`, `medium`, `high`,
or `auto`. Default: `auto`.
  QualityEnum quality = 9;
  // The size of the generated image. One of `1024x1024`, `1024x1536`,
`1536x1024`, or `auto`. Default: `auto`.
  enum SizeEnum {
    SIZEENUM_UNSPECIFIED = 0;
    1024X1024 = 1;
    1024X1536 = 2;
    1536X1024 = 3;
    AUTO = 4;
  }
  

  // The size of the generated image. One of `1024x1024`, `1024x1536`,
`1536x1024`, or `auto`. Default: `auto`.
  SizeEnum size = 10;
  // The type of the image generation tool. Always `image_generation`.
  enum TypeEnum {
    TYPEENUM_UNSPECIFIED = 0;
    IMAGE_GENERATION = 1;
  }
  

  // The type of the image generation tool. Always `image_generation`.
  TypeEnum type = 11;
}
