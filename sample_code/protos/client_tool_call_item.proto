// GENERATED CODE - DO NOT EDIT
// Generated by schema2code proto generator

syntax = "proto3";

package schema;

import "client_tool_call_status.proto";

// Record of a client side tool invocation initiated by the assistant.
message ClientToolCallItem {
  // JSON-encoded arguments that were sent to the tool.
  string arguments = 1;
  // Identifier for the client tool call.
  string call_id = 2;
  // Unix timestamp (in seconds) for when the item was created.
  int32 created_at = 3;
  // Identifier of the thread item.
  string id = 4;
  // Tool name that was invoked.
  string name = 5;
  // Type discriminator that is always `chatkit.thread_item`.
  enum ObjectEnum {
    OBJECTENUM_UNSPECIFIED = 0;
    CHATKIT_THREAD_ITEM = 1;
  }
  

  // Type discriminator that is always `chatkit.thread_item`.
  ObjectEnum object = 6;
  string output = 7;
  // Execution status for the tool call.
  ClientToolCallStatus status = 8;
  // Identifier of the parent thread.
  string thread_id = 9;
  // Type discriminator that is always `chatkit.client_tool_call`.
  enum TypeEnum {
    TYPEENUM_UNSPECIFIED = 0;
    CHATKIT_CLIENT_TOOL_CALL = 1;
  }
  

  // Type discriminator that is always `chatkit.client_tool_call`.
  TypeEnum type = 10;
}
