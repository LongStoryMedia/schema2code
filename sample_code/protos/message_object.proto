// GENERATED CODE - DO NOT EDIT
// Generated by schema2code proto generator

syntax = "proto3";

package schema;

import "message_content.proto";
import "metadata.proto";

// Represents a message within a [thread](https://platform.openai.com/docs/api-reference/threads).
message MessageObject {
  string assistant_id = 1;
  message AttachmentsItem {
    // The ID of the file to attach to the message.
    string file_id = 1;
    // The tools to add this file to.
    repeated AssistantToolsCode tools = 2;
  }
  

  repeated AttachmentsItem attachments = 2;
  int32 completed_at = 3;
  // The content of the message in array of text and/or images.
  repeated MessageContent content = 4;
  // The Unix timestamp (in seconds) for when the message was created.
  int32 created_at = 5;
  // The identifier, which can be referenced in API endpoints.
  string id = 6;
  int32 incomplete_at = 7;
  // On an incomplete message, details about why the message is incomplete.
  message IncompleteDetails {
  message IncompleteDetails {
    // The reason the message is incomplete.
    enum ReasonEnum {
      REASONENUM_UNSPECIFIED = 0;
      CONTENT_FILTER = 1;
      MAX_TOKENS = 2;
      RUN_CANCELLED = 3;
      RUN_EXPIRED = 4;
      RUN_FAILED = 5;
    }
    
  
    // The reason the message is incomplete.
    ReasonEnum reason = 1;
  }
  

  IncompleteDetails incomplete_details = 8;
  Metadata metadata = 9;
  // The object type, which is always `thread.message`.
  enum ObjectEnum {
    OBJECTENUM_UNSPECIFIED = 0;
    THREAD_MESSAGE = 1;
  }
  

  // The object type, which is always `thread.message`.
  ObjectEnum object = 10;
  // The entity that produced the message. One of `user` or `assistant`.
  enum RoleEnum {
    ROLEENUM_UNSPECIFIED = 0;
    USER = 1;
    ASSISTANT = 2;
  }
  

  // The entity that produced the message. One of `user` or `assistant`.
  RoleEnum role = 11;
  string run_id = 12;
  // The status of the message, which can be either `in_progress`, `incomplete`, or `completed`.
  enum StatusEnum {
    STATUSENUM_UNSPECIFIED = 0;
    IN_PROGRESS = 1;
    INCOMPLETE = 2;
    COMPLETED = 3;
  }
  

  // The status of the message, which can be either `in_progress`, `incomplete`, or `completed`.
  StatusEnum status = 13;
  // The [thread](https://platform.openai.com/docs/api-reference/threads) ID that this message belongs to.
  string thread_id = 14;
}
