# Code generated by schema2code at 2025-08-03 11:47:18. DO NOT EDIT.


from typing import List, Dict, Optional, Any, Union
from datetime import datetime, date, time, timedelta
import uuid
from pydantic import BaseModel, Field, AnyUrl, EmailStr



"""Model profile configuration"""
class ModelProfileConfig(BaseModel):
    # Primary profile ID
    primary_profile_id: uuid.UUID = Field(..., description="Primary profile ID")
    # Summarization profile ID
    summarization_profile_id: uuid.UUID = Field(..., description="Summarization profile ID")
    # Master summary profile ID
    master_summary_profile_id: uuid.UUID = Field(..., description="Master summary profile ID")
    # Brief summary profile ID
    brief_summary_profile_id: uuid.UUID = Field(..., description="Brief summary profile ID")
    # Key points profile ID
    key_points_profile_id: uuid.UUID = Field(..., description="Key points profile ID")
    # Improvement profile ID
    improvement_profile_id: uuid.UUID = Field(..., description="Improvement profile ID")
    # Analysis profile ID
    analysis_profile_id: uuid.UUID = Field(..., description="Analysis profile ID")
    # Memory retrieval profile ID
    memory_retrieval_profile_id: uuid.UUID = Field(..., description="Memory retrieval profile ID")
    # Self-critique profile ID
    self_critique_profile_id: uuid.UUID = Field(..., description="Self-critique profile ID")
    # Research task profile ID
    research_task_profile_id: uuid.UUID = Field(..., description="Research task profile ID")
    # Research plan profile ID
    research_plan_profile_id: uuid.UUID = Field(..., description="Research plan profile ID")
    # Research consolidation profile ID
    research_consolidation_profile_id: uuid.UUID = Field(..., description="Research consolidation profile ID")
    # Research analysis profile ID
    research_analysis_profile_id: uuid.UUID = Field(..., description="Research analysis profile ID")
    # Embedding profile ID
    embedding_profile_id: uuid.UUID = Field(..., description="Embedding profile ID")
    # Formatting profile ID
    formatting_profile_id: uuid.UUID = Field(..., description="Formatting profile ID")
    # Image generation prompt profile ID
    image_generation_prompt_profile_id: uuid.UUID = Field(..., description="Image generation prompt profile ID")
    # Image generation profile ID
    image_generation_profile_id: uuid.UUID = Field(..., description="Image generation profile ID")

    class Config:
        extra = "ignore"