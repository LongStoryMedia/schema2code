# Code generated by schema2code at 2025-08-03 11:47:19. DO NOT EDIT.


from typing import List, Dict, Optional, Any, Union
from datetime import datetime, date, time, timedelta
from pydantic import BaseModel, Field, AnyUrl, EmailStr



"""WebSocketConnection represents an active WebSocket connection"""
class WebSocketConnection(BaseModel):
    # Unique identifier for the connection
    id: str = Field(..., description="Unique identifier for the connection")
    # Identifier of the user associated with the connection
    user_id: str = Field(..., description="Identifier of the user associated with the connection")
    # Type of WebSocket connection (e.g., chat, image, status)
    type: str = Field(..., description="Type of WebSocket connection (e.g., chat, image, status)")
    # The WebSocket connection object (represented as string)
    conn: Optional[Any] = Field(None, description="The WebSocket connection object (represented as string)")
    # Timestamp indicating when the connection was established
    created_at: datetime = Field(..., description="Timestamp indicating when the connection was established")

    class Config:
        extra = "ignore"