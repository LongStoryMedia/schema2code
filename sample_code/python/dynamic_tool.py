# Code generated by schema2code at 2025-09-18 14:56:21. DO NOT EDIT.


from typing import List, Dict, Optional, Any, Union
from datetime import datetime, date, time, timedelta
from pydantic import BaseModel, Field, AnyUrl, EmailStr, conint, confloat



class DynamicTool(BaseModel):
    """Schema for DynamicTool object"""
    id: Optional[int] = Field(default=None, description="The unique identifier for the tool")
    """The unique identifier for the tool"""
    user_id: Any = Field(..., description="The unique identifier for the user")
    """The unique identifier for the user"""
    name: str = Field(..., description="The name of the tool")
    """The name of the tool"""
    description: str = Field(..., description="Description of what the tool does")
    """Description of what the tool does"""
    embedding: Optional[List[float]] = Field(default=None, description="The tool's embedding vector")
    """The tool's embedding vector"""
    code: str = Field(..., description="The tool's implementation code")
    """The tool's implementation code"""
    function_name: str = Field(..., description="The name of the function to be called")
    """The name of the function to be called"""
    parameters: Optional[Dict[str, Any]] = Field(default=None, description="Parameters for the tool with any values")
    """Parameters for the tool with any values"""
    created_at: Optional[datetime] = Field(default=None, description="The timestamp when the tool was created")
    """The timestamp when the tool was created"""
    updated_at: Optional[datetime] = Field(default=None, description="The timestamp when the tool was last updated")
    """The timestamp when the tool was last updated"""

    class Config:
        extra = "ignore"