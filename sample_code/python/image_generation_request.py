# Code generated by schema2code at 2025-08-03 11:47:12. DO NOT EDIT.


from typing import List, Dict, Optional, Any, Union
from datetime import datetime, date, time, timedelta
from pydantic import BaseModel, Field, AnyUrl, EmailStr



"""Request for generating an image"""
class ImageGenerateRequest(BaseModel):
    # The prompt for image generation
    prompt: str = Field(..., description="The prompt for image generation")
    # Width of the generated image
    width: Optional[int] = Field(None, description="Width of the generated image")
    # Height of the generated image
    height: Optional[int] = Field(None, description="Height of the generated image")
    # Number of inference steps
    inference_steps: Optional[int] = Field(None, description="Number of inference steps")
    # Guidance scale for image generation
    guidance_scale: Optional[float] = Field(None, description="Guidance scale for image generation")
    # Whether to use low memory mode
    low_memory_mode: Optional[bool] = Field(None, description="Whether to use low memory mode")
    # Negative prompt for image generation
    negative_prompt: Optional[str] = Field(None, description="Negative prompt for image generation")
    # Model to use for image generation
    model: str = Field(..., description="Model to use for image generation")
    # List of LoRA models to apply to the image generation
    loras: Optional[List[str]] = Field(None, description="List of LoRA models to apply to the image generation")
    # ID of the image to use as a base for image generation (for img2img)
    image_id: Optional[int] = Field(None, description="ID of the image to use as a base for image generation (for img2img)")
    # URL of the image to use as a base for image generation (for img2img)
    url: Optional[str] = Field(None, description="URL of the image to use as a base for image generation (for img2img)")
    # Name of the file to save the generated image as
    filename: Optional[str] = Field(None, description="Name of the file to save the generated image as")

    class Config:
        extra = "ignore"