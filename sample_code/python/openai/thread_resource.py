# Code generated by schema2code at 2026-02-23 18:05:01. DO NOT EDIT.


from __future__ import annotations
from typing import List, Dict, Optional, Any, Union, Annotated, Literal
from datetime import datetime, date, time, timedelta
from pydantic import BaseModel, Field, AnyUrl, EmailStr, conint, confloat



class ActiveStatus(BaseModel):
    """Indicates that a thread is active."""
    type: Annotated[Literal["active"], Field(default='active', description="Status discriminator that is always `active`.")]
    """Status discriminator that is always `active`."""

    class Config:
        extra = "ignore"

class ClosedStatus(BaseModel):
    """Indicates that a thread has been closed."""
    type: Annotated[Literal["closed"], Field(default='closed', description="Status discriminator that is always `closed`.")]
    """Status discriminator that is always `closed`."""
    reason: Annotated[Union[str, Any], Field(...)]

    class Config:
        extra = "ignore"

class LockedStatus(BaseModel):
    """Indicates that a thread is locked and cannot accept new input."""
    type: Annotated[Literal["locked"], Field(default='locked', description="Status discriminator that is always `locked`.")]
    """Status discriminator that is always `locked`."""
    reason: Annotated[Union[str, Any], Field(...)]

    class Config:
        extra = "ignore"

class TheThreadObject(BaseModel):
    """Represents a ChatKit thread and its current status."""
    id: Annotated[str, Field(..., description="Identifier of the thread.")]
    """Identifier of the thread."""
    object: Annotated[Literal["chatkit.thread"], Field(default='chatkit.thread', description="Type discriminator that is always `chatkit.thread`.")]
    """Type discriminator that is always `chatkit.thread`."""
    created_at: Annotated[int, Field(..., description="Unix timestamp (in seconds) for when the thread was created.")]
    """Unix timestamp (in seconds) for when the thread was created."""
    title: Annotated[Union[str, Any], Field(...)]
    status: Annotated[Union[ActiveStatus, LockedStatus, ClosedStatus], Field(..., description="Current status for the thread. Defaults to `active` for newly created threads.")]
    """Current status for the thread. Defaults to `active` for newly created threads."""
    user: Annotated[str, Field(..., description="Free-form string that identifies your end user who owns the thread.")]
    """Free-form string that identifies your end user who owns the thread."""

    class Config:
        extra = "ignore"

TheThreadObject.model_rebuild()