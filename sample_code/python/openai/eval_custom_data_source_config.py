# Code generated by schema2code at 2026-02-23 14:46:42. DO NOT EDIT.


from __future__ import annotations
from typing import List, Dict, Optional, Any, Union, Annotated, Literal
from datetime import datetime, date, time, timedelta
from pydantic import BaseModel, Field, AnyUrl, EmailStr, conint, confloat



class EvalCustomDataSourceConfig(BaseModel):
    """A CustomDataSourceConfig which specifies the schema of your `item` and optionally `sample` namespaces.
The response schema defines the shape of the data that will be:
- Used to define your testing criteria and
- What data is required when creating a run
"""
    schema: Annotated[Dict[str, Any], Field(..., description="The json schema for the run data source items. Learn how to build JSON schemas [here](https://json-schema.org/). ")]
    """The json schema for the run data source items. Learn how to build JSON schemas [here](https://json-schema.org/). """
    type: Annotated[Literal["custom"], Field(default='custom', description="The type of data source. Always `custom`.")]
    """The type of data source. Always `custom`."""

    class Config:
        extra = "ignore"