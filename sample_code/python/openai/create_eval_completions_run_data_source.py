# Code generated by schema2code at 2026-02-23 18:05:01. DO NOT EDIT.


from __future__ import annotations
from typing import List, Dict, Optional, Any, Union, Annotated, Literal
from datetime import datetime, date, time, timedelta
from pydantic import BaseModel, Field, AnyUrl, EmailStr, conint, confloat



class ChatCompletionTool(BaseModel):
    """A function tool that can be used to generate a response.
"""
    type: Annotated[Literal["function"], Field(..., description="The type of the tool. Currently, only `function` is supported.")]
    """The type of the tool. Currently, only `function` is supported."""
    function: Annotated[FunctionObject, Field(...)]

    class Config:
        extra = "ignore"

class FunctionObject(BaseModel):
    description: Annotated[Optional[str], Field(default=None, description="A description of what the function does, used by the model to choose when and how to call the function.")] = None
    """A description of what the function does, used by the model to choose when and how to call the function."""
    name: Annotated[str, Field(..., description="The name of the function to be called. Must be a-z, A-Z, 0-9, or contain underscores and dashes, with a maximum length of 64.")]
    """The name of the function to be called. Must be a-z, A-Z, 0-9, or contain underscores and dashes, with a maximum length of 64."""
    parameters: Annotated[Optional[FunctionParameters], Field(default=None)] = None
    strict: Annotated[Optional[Union[bool, Any]], Field(default=None)] = None

    class Config:
        extra = "ignore"

class FunctionParameters(BaseModel):
    """The parameters the functions accepts, described as a JSON Schema object. See the [guide](https://platform.openai.com/docs/guides/function-calling) for examples, and the [JSON Schema reference](https://json-schema.org/understanding-json-schema/) for documentation about the format. 

Omitting `parameters` defines a function with an empty parameter list."""
    pass

ReasoningEffort = Union[Literal["none", "minimal", "low", "medium", "high", "xhigh"]]

class ResponseFormatText(BaseModel):
    """Default response format. Used to generate text responses.
"""
    type: Annotated[Literal["text"], Field(..., description="The type of response format being defined. Always `text`.")]
    """The type of response format being defined. Always `text`."""

    class Config:
        extra = "ignore"

class EvalItem(BaseModel):
    """A message input to the model with a role indicating instruction following
hierarchy. Instructions given with the `developer` or `system` role take
precedence over instructions given with the `user` role. Messages with the
`assistant` role are presumed to have been generated by the model in previous
interactions.
"""
    role: Annotated[Literal["user", "assistant", "system", "developer"], Field(..., description="The role of the message input. One of `user`, `assistant`, `system`, or `developer`. ")]
    """The role of the message input. One of `user`, `assistant`, `system`, or `developer`. """
    content: Annotated[EvalItemContent, Field(...)]
    type: Annotated[Optional[Literal["message"]], Field(default=None, description="The type of the message input. Always `message`. ")] = None
    """The type of the message input. Always `message`. """

    class Config:
        extra = "ignore"

EvalItemContent = Union[EvalItemContentItem, EvalItemContentArray]

class EvalItemContentArray(BaseModel):
    """A list of inputs, each of which may be either an input text, output text, input
image, or input audio object.
"""
    pass

EvalItemContentItem = Union[EvalItemContentText, InputTextContent, EvalItemContentOutputText, EvalItemInputImage, InputAudio]

class InputTextContent(BaseModel):
    """A text input to the model."""
    type: Annotated[Literal["input_text"], Field(default='input_text', description="The type of the input item. Always `input_text`.")]
    """The type of the input item. Always `input_text`."""
    text: Annotated[str, Field(..., description="The text input to the model.")]
    """The text input to the model."""

    class Config:
        extra = "ignore"

class EvalItemInputImage(BaseModel):
    """An image input block used within EvalItem content arrays."""
    type: Annotated[Literal["input_image"], Field(..., description="The type of the image input. Always `input_image`. ")]
    """The type of the image input. Always `input_image`. """
    image_url: Annotated[str, Field(..., description="The URL of the image input. ")]
    """The URL of the image input. """
    detail: Annotated[Optional[str], Field(default=None, description="The detail level of the image to be sent to the model. One of `high`, `low`, or `auto`. Defaults to `auto`. ")] = None
    """The detail level of the image to be sent to the model. One of `high`, `low`, or `auto`. Defaults to `auto`. """

    class Config:
        extra = "ignore"

class InputAudio(BaseModel):
    """An audio input to the model.
"""
    type: Annotated[Literal["input_audio"], Field(..., description="The type of the input item. Always `input_audio`. ")]
    """The type of the input item. Always `input_audio`. """
    input_audio: Annotated[InputAudio, Field(...)]

    class Config:
        extra = "ignore"

class EvalItemContentOutputText(BaseModel):
    """A text output from the model.
"""
    type: Annotated[Literal["output_text"], Field(..., description="The type of the output text. Always `output_text`. ")]
    """The type of the output text. Always `output_text`. """
    text: Annotated[str, Field(..., description="The text output from the model. ")]
    """The text output from the model. """

    class Config:
        extra = "ignore"

class EvalItemContentText(BaseModel):
    """A text input to the model.
"""
    pass

class ResponseFormatJsonSchema(BaseModel):
    """JSON Schema response format. Used to generate structured JSON responses.
Learn more about [Structured Outputs](https://platform.openai.com/docs/guides/structured-outputs).
"""
    type: Annotated[Literal["json_schema"], Field(..., description="The type of response format being defined. Always `json_schema`.")]
    """The type of response format being defined. Always `json_schema`."""
    json_schema: Annotated[JsonSchema, Field(..., description="Structured Outputs configuration options, including a JSON Schema. ")]
    """Structured Outputs configuration options, including a JSON Schema. """

    class Config:
        extra = "ignore"

class ResponseFormatJsonSchemaSchema(BaseModel):
    """The schema for the response format, described as a JSON Schema object.
Learn how to build JSON schemas [here](https://json-schema.org/).
"""
    pass

class EvalStoredCompletionsSource(BaseModel):
    """A StoredCompletionsRunDataSource configuration describing a set of filters
"""
    type: Annotated[Literal["stored_completions"], Field(default='stored_completions', description="The type of source. Always `stored_completions`.")]
    """The type of source. Always `stored_completions`."""
    metadata: Annotated[Optional[Metadata], Field(default=None)] = None
    model: Annotated[Optional[Union[str, Any]], Field(default=None)] = None
    created_after: Annotated[Optional[Union[int, Any]], Field(default=None)] = None
    created_before: Annotated[Optional[Union[int, Any]], Field(default=None)] = None
    limit: Annotated[Optional[Union[int, Any]], Field(default=None)] = None

    class Config:
        extra = "ignore"

class Metadata(BaseModel):
    pass

class EvalJsonlFileIdSource(BaseModel):
    type: Annotated[Literal["file_id"], Field(default='file_id', description="The type of jsonl source. Always `file_id`.")]
    """The type of jsonl source. Always `file_id`."""
    id: Annotated[str, Field(..., description="The identifier of the file.")]
    """The identifier of the file."""

    class Config:
        extra = "ignore"

class EvalJsonlFileContentSource(BaseModel):
    type: Annotated[Literal["file_content"], Field(default='file_content', description="The type of jsonl source. Always `file_content`.")]
    """The type of jsonl source. Always `file_content`."""
    content: Annotated[List[ContentItem], Field(..., description="The content of the jsonl file.")]
    """The content of the jsonl file."""

    class Config:
        extra = "ignore"

class ResponseFormatJsonObject(BaseModel):
    """JSON object response format. An older method of generating JSON responses.
Using `json_schema` is recommended for models that support it. Note that the
model will not generate JSON without a system or user message instructing it
to do so.
"""
    type: Annotated[Literal["json_object"], Field(..., description="The type of response format being defined. Always `json_object`.")]
    """The type of response format being defined. Always `json_object`."""

    class Config:
        extra = "ignore"

class EasyInputMessage(BaseModel):
    """A message input to the model with a role indicating instruction following
hierarchy. Instructions given with the `developer` or `system` role take
precedence over instructions given with the `user` role. Messages with the
`assistant` role are presumed to have been generated by the model in previous
interactions.
"""
    role: Annotated[Literal["user", "assistant", "system", "developer"], Field(..., description="The role of the message input. One of `user`, `assistant`, `system`, or `developer`. ")]
    """The role of the message input. One of `user`, `assistant`, `system`, or `developer`. """
    content: Annotated[Union[str, InputMessageContentList], Field(..., description="Text, image, or audio input to the model, used to generate a response. Can also contain previous assistant responses. ")]
    """Text, image, or audio input to the model, used to generate a response. Can also contain previous assistant responses. """
    type: Annotated[Optional[Literal["message"]], Field(default=None, description="The type of the message input. Always `message`. ")] = None
    """The type of the message input. Always `message`. """

    class Config:
        extra = "ignore"

class InputMessageContentList(BaseModel):
    """A list of one or many input items to the model, containing different content 
types.
"""
    pass

InputContent = Union[InputTextContent, InputImageContent, InputFileContent]

class InputImageContent(BaseModel):
    """An image input to the model. Learn about [image inputs](https://platform.openai.com/docs/guides/vision)."""
    type: Annotated[Literal["input_image"], Field(default='input_image', description="The type of the input item. Always `input_image`.")]
    """The type of the input item. Always `input_image`."""
    image_url: Annotated[Optional[Union[str, Any]], Field(default=None)] = None
    file_id: Annotated[Optional[Union[str, Any]], Field(default=None)] = None
    detail: Annotated[ImageDetail, Field(..., description="The detail level of the image to be sent to the model. One of `high`, `low`, or `auto`. Defaults to `auto`.")]
    """The detail level of the image to be sent to the model. One of `high`, `low`, or `auto`. Defaults to `auto`."""

    class Config:
        extra = "ignore"

class ImageDetail(str, Enum):
    LOW = 'low'
    HIGH = 'high'
    AUTO = 'auto'

class InputFileContent(BaseModel):
    """A file input to the model."""
    type: Annotated[Literal["input_file"], Field(default='input_file', description="The type of the input item. Always `input_file`.")]
    """The type of the input item. Always `input_file`."""
    file_id: Annotated[Optional[Union[str, Any]], Field(default=None)] = None
    filename: Annotated[Optional[str], Field(default=None, description="The name of the file to be sent to the model.")] = None
    """The name of the file to be sent to the model."""
    file_url: Annotated[Optional[str], Field(default=None, description="The URL of the file to be sent to the model.")] = None
    """The URL of the file to be sent to the model."""
    file_data: Annotated[Optional[str], Field(default=None, description="The content of the file to be sent to the model. ")] = None
    """The content of the file to be sent to the model. """

    class Config:
        extra = "ignore"

class TemplateInputMessages(BaseModel):
    type: Annotated[Literal["template"], Field(..., description="The type of input messages. Always `template`.")]
    """The type of input messages. Always `template`."""
    template: Annotated[List[Union[EasyInputMessage, EvalItem]], Field(..., description="A list of chat messages forming the prompt or context. May include variable references to the `item` namespace, ie {{item.name}}.")]
    """A list of chat messages forming the prompt or context. May include variable references to the `item` namespace, ie {{item.name}}."""

    class Config:
        extra = "ignore"

class ItemReferenceInputMessages(BaseModel):
    type: Annotated[Literal["item_reference"], Field(..., description="The type of input messages. Always `item_reference`.")]
    """The type of input messages. Always `item_reference`."""
    item_reference: Annotated[str, Field(..., description="A reference to a variable in the `item` namespace. Ie, \"item.input_trajectory\"")]
    """A reference to a variable in the `item` namespace. Ie, \"item.input_trajectory\""""

    class Config:
        extra = "ignore"

class SamplingParams(BaseModel):
    reasoning_effort: Annotated[Optional[ReasoningEffort], Field(default=None)] = None
    temperature: Annotated[Optional[float], Field(default=1, description="A higher temperature increases randomness in the outputs.")] = 1
    """A higher temperature increases randomness in the outputs."""
    max_completion_tokens: Annotated[Optional[int], Field(default=None, description="The maximum number of tokens in the generated output.")] = None
    """The maximum number of tokens in the generated output."""
    top_p: Annotated[Optional[float], Field(default=1, description="An alternative to temperature for nucleus sampling; 1.0 includes all tokens.")] = 1
    """An alternative to temperature for nucleus sampling; 1.0 includes all tokens."""
    seed: Annotated[Optional[int], Field(default=42, description="A seed value to initialize the randomness, during sampling.")] = 42
    """A seed value to initialize the randomness, during sampling."""
    response_format: Annotated[Optional[Union[ResponseFormatText, ResponseFormatJsonSchema, ResponseFormatJsonObject]], Field(default=None, description="An object specifying the format that the model must output.  Setting to `{ \"type\": \"json_schema\", \"json_schema\": {...} }` enables Structured Outputs which ensures the model will match your supplied JSON schema. Learn more in the [Structured Outputs guide](https://platform.openai.com/docs/guides/structured-outputs).  Setting to `{ \"type\": \"json_object\" }` enables the older JSON mode, which ensures the message the model generates is valid JSON. Using `json_schema` is preferred for models that support it. ")] = None
    """An object specifying the format that the model must output.  Setting to `{ \"type\": \"json_schema\", \"json_schema\": {...} }` enables Structured Outputs which ensures the model will match your supplied JSON schema. Learn more in the [Structured Outputs guide](https://platform.openai.com/docs/guides/structured-outputs).  Setting to `{ \"type\": \"json_object\" }` enables the older JSON mode, which ensures the message the model generates is valid JSON. Using `json_schema` is preferred for models that support it. """
    tools: Annotated[Optional[List[ChatCompletionTool]], Field(default=None, description="A list of tools the model may call. Currently, only functions are supported as a tool. Use this to provide a list of functions the model may generate JSON inputs for. A max of 128 functions are supported. ")] = None
    """A list of tools the model may call. Currently, only functions are supported as a tool. Use this to provide a list of functions the model may generate JSON inputs for. A max of 128 functions are supported. """

    class Config:
        extra = "ignore"

class CompletionsRunDataSource(BaseModel):
    """A CompletionsRunDataSource object describing a model sampling configuration.
"""
    type: Annotated[Literal["completions"], Field(default='completions', description="The type of run data source. Always `completions`.")]
    """The type of run data source. Always `completions`."""
    input_messages: Annotated[Optional[Union[TemplateInputMessages, ItemReferenceInputMessages]], Field(default=None, description="Used when sampling from a model. Dictates the structure of the messages passed into the model. Can either be a reference to a prebuilt trajectory (ie, `item.input_trajectory`), or a template with variable references to the `item` namespace.")] = None
    """Used when sampling from a model. Dictates the structure of the messages passed into the model. Can either be a reference to a prebuilt trajectory (ie, `item.input_trajectory`), or a template with variable references to the `item` namespace."""
    sampling_params: Annotated[Optional[SamplingParams], Field(default=None)] = None
    model: Annotated[Optional[str], Field(default=None, description="The name of the model to use for generating completions (e.g. \"o3-mini\").")] = None
    """The name of the model to use for generating completions (e.g. \"o3-mini\")."""
    source: Annotated[Union[EvalJsonlFileContentSource, EvalJsonlFileIdSource, EvalStoredCompletionsSource], Field(..., description="Determines what populates the `item` namespace in this run's data source.")]
    """Determines what populates the `item` namespace in this run's data source."""

    class Config:
        extra = "ignore"

CompletionsRunDataSource.model_rebuild()