# Code generated by schema2code at 2026-02-23 14:46:39. DO NOT EDIT.


from __future__ import annotations
from typing import List, Dict, Optional, Any, Union, Annotated, Literal
from datetime import datetime, date, time, timedelta
from pydantic import BaseModel, Field, AnyUrl, EmailStr, conint, confloat



class CreateEvalCustomDataSourceConfig(BaseModel):
    """A CustomDataSourceConfig object that defines the schema for the data source used for the evaluation runs.
This schema is used to define the shape of the data that will be:
- Used to define your testing criteria and
- What data is required when creating a run
"""
    include_sample_schema: Annotated[Optional[bool], Field(default=False, description="Whether the eval should expect you to populate the sample namespace (ie, by generating responses off of your data source)")] = False
    """Whether the eval should expect you to populate the sample namespace (ie, by generating responses off of your data source)"""
    item_schema: Annotated[Dict[str, Any], Field(..., description="The json schema for each row in the data source.")]
    """The json schema for each row in the data source."""
    type: Annotated[Literal["custom"], Field(default='custom', description="The type of data source. Always `custom`.")]
    """The type of data source. Always `custom`."""

    class Config:
        extra = "ignore"