# Code generated by schema2code at 2025-08-03 11:47:15. DO NOT EDIT.


from typing import List, Dict, Optional, Any, Union
from datetime import datetime, date, time, timedelta
from pydantic import BaseModel, Field, AnyUrl, EmailStr



"""Parameters for configuring a language model"""
class ModelParameters(BaseModel):
    # Size of the context window
    num_ctx: Optional[int] = Field(None, description="Size of the context window")
    # Number of tokens to consider for repetition penalties
    repeat_last_n: Optional[int] = Field(None, description="Number of tokens to consider for repetition penalties")
    # Penalty for repetitions
    repeat_penalty: Optional[float] = Field(None, description="Penalty for repetitions")
    # Sampling temperature; higher values produce more creative outputs
    temperature: Optional[float] = Field(None, description="Sampling temperature; higher values produce more creative outputs")
    # Random seed for reproducibility
    seed: Optional[int] = Field(None, description="Random seed for reproducibility")
    # Sequences where the model should stop generating
    stop: Optional[List[str]] = Field(None, description="Sequences where the model should stop generating")
    # Maximum number of tokens to predict
    num_predict: Optional[int] = Field(None, description="Maximum number of tokens to predict")
    # Limits next token selection to top K options
    top_k: Optional[int] = Field(None, description="Limits next token selection to top K options")
    # Limits next token selection to tokens comprising the top P probability mass (nucleus sampling)
    top_p: Optional[float] = Field(None, description="Limits next token selection to tokens comprising the top P probability mass (nucleus sampling)")
    # Minimum probability threshold for token selection
    min_p: Optional[float] = Field(None, description="Minimum probability threshold for token selection")

    class Config:
        extra = "ignore"