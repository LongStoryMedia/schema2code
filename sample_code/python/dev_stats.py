# Code generated by schema2code at 2025-08-03 11:47:10. DO NOT EDIT.


from typing import List, Dict, Optional, Any, Union
from datetime import datetime, date, time, timedelta
from pydantic import BaseModel, Field, AnyUrl, EmailStr



"""Schema representing device memory statistics."""
class DevStats(BaseModel):
    # Device identifier.
    id: str = Field(..., description="Device identifier.")
    # Universally unique identifier for the device.
    uuid: str = Field(..., description="Universally unique identifier for the device.")
    # GPU utilization percentage.
    gpu_util: float = Field(..., description="GPU utilization percentage.")
    # Memory utilization percentage.
    mem_util: float = Field(..., description="Memory utilization percentage.")
    # Total memory in megabytes.
    mem_total: float = Field(..., description="Total memory in megabytes.")
    # Used memory in megabytes.
    mem_used: float = Field(..., description="Used memory in megabytes.")
    # Free memory in megabytes.
    mem_free: float = Field(..., description="Free memory in megabytes.")
    # Driver version.
    driver: str = Field(..., description="Driver version.")
    # Device name.
    name: str = Field(..., description="Device name.")
    # Device serial number.
    serial: str = Field(..., description="Device serial number.")
    # Display mode status.
    display_mode: str = Field(..., description="Display mode status.")
    # Display active status.
    display_active: str = Field(..., description="Display active status.")
    # Device temperature in Celsius.
    temperature: float = Field(..., description="Device temperature in Celsius.")

    class Config:
        extra = "ignore"