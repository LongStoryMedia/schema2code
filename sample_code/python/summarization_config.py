# Code generated by schema2code at 2025-08-03 11:47:18. DO NOT EDIT.


from typing import List, Dict, Optional, Any, Union
from datetime import datetime, date, time, timedelta
from pydantic import BaseModel, Field, AnyUrl, EmailStr



"""Conversation summarization settings"""
class SummarizationConfig(BaseModel):
    # Whether summarization is enabled
    enabled: bool = Field(..., description="Whether summarization is enabled")
    # Number of messages before summary
    messages_before_summary: int = Field(..., description="Number of messages before summary")
    # Number of summaries before consolidation
    summaries_before_consolidation: int = Field(..., description="Number of summaries before consolidation")
    # Embedding dimension
    embedding_dimension: int = Field(..., description="Embedding dimension")
    # Maximum summary levels
    max_summary_levels: int = Field(..., description="Maximum summary levels")
    # Summary weight coefficient
    summary_weight_coefficient: float = Field(..., description="Summary weight coefficient")

    class Config:
        extra = "ignore"