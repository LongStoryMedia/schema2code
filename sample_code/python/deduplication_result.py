# Code generated by schema2code at 2025-09-18 14:56:01. DO NOT EDIT.


from typing import List, Dict, Optional, Any, Union
from datetime import datetime, date, time, timedelta
from .dynamic_tool import DynamicTool
from pydantic import BaseModel, Field, AnyUrl, EmailStr, conint, confloat



class DeduplicationResult(BaseModel):
    """Result of tool deduplication analysis"""
    is_duplicate: bool = Field(..., description="Whether the proposed tool is a duplicate")
    """Whether the proposed tool is a duplicate"""
    existing_tool: Optional[DynamicTool] = Field(default=None, description="Existing similar tool, if found")
    """Existing similar tool, if found"""
    similarity_score: float = Field(..., description="Similarity score with existing tool (0-1)", ge=0.0, le=1.0)
    """Similarity score with existing tool (0-1)"""
    recommendation: str = Field(..., description="Recommendation for handling the duplication")
    """Recommendation for handling the duplication"""
    should_create_new: bool = Field(..., description="Whether a new tool should be created")
    """Whether a new tool should be created"""
    merge_suggestion: Optional[str] = Field(default=None, description="Optional suggestion for merging functionality")
    """Optional suggestion for merging functionality"""

    class Config:
        extra = "ignore"