# Code generated by schema2code at 2025-08-03 11:47:18. DO NOT EDIT.


from typing import List, Dict, Optional, Any, Union
from datetime import datetime, date, time, timedelta
from pydantic import BaseModel, Field, AnyUrl, EmailStr



"""Image generation configuration"""
class ImageGenerationConfig(BaseModel):
    # Whether image generation is enabled
    enabled: bool = Field(..., description="Whether image generation is enabled")
    # Directory for file storage
    storage_directory: str = Field(..., description="Directory for file storage")
    # Maximum number of pixels in width or height (whichever is greater) for generated images
    max_image_size: int = Field(..., description="Maximum number of pixels in width or height (whichever is greater) for generated images")
    # Retention period for generated images in hours (e.g., 720 for 30 days)
    retention_hours: int = Field(..., description="Retention period for generated images in hours (e.g., 720 for 30 days)")
    # Whether to enable automatic prompt refinement for image generation. This will take the given prompt and attempt to refine it to improve the quality of the generated image.
    auto_prompt_refinement: bool = Field(..., description="Whether to enable automatic prompt refinement for image generation. This will take the given prompt and attempt to refine it to improve the quality of the generated image.")
    # Width of the generated image
    width: int = Field(..., description="Width of the generated image")
    # Height of the generated image
    height: int = Field(..., description="Height of the generated image")
    # Number of inference steps
    inference_steps: int = Field(..., description="Number of inference steps")
    # Guidance scale for image generation
    guidance_scale: float = Field(..., description="Guidance scale for image generation")
    # Whether to use low memory mode
    low_memory_mode: bool = Field(..., description="Whether to use low memory mode")
    # Negative prompt for image generation
    negative_prompt: str = Field(..., description="Negative prompt for image generation")

    class Config:
        extra = "ignore"