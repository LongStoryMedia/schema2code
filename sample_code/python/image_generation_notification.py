# Code generated by schema2code at 2025-08-03 11:47:11. DO NOT EDIT.


from typing import List, Dict, Optional, Any, Union
from datetime import datetime, date, time, timedelta
from .image_metadata import ImageMetadata
from pydantic import BaseModel, Field, AnyUrl, EmailStr



"""Notification sent to clients when an image is generated."""
class ImageGenerationNotification(BaseModel):
    # ID of the generated image
    image_id: Optional[str] = Field(None, description="ID of the generated image")
    # URL to download the image
    download_url: str = Field(..., description="URL to download the image")
    # URL to view a thumbnail
    thumbnail_url: Optional[str] = Field(None, description="URL to view a thumbnail")
    # Base64 encoded image data if applicable
    b64_data: Optional[str] = Field(None, description="Base64 encoded image data if applicable")
    # Additional metadata related to the image generation
    metadata: Optional[ImageMetadata] = Field(None, description="Additional metadata related to the image generation")

    class Config:
        extra = "ignore"