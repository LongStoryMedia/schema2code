# Code generated by schema2code at 2025-08-26 22:17:48. DO NOT EDIT.


from typing import List, Dict, Optional, Any, Union
from datetime import datetime, date, time, timedelta
from .image_metadata import ImageMetadata
from pydantic import BaseModel, Field, AnyUrl, EmailStr, conint, confloat



class ImageGenerationNotification(BaseModel):
    """Notification sent to clients when an image is generated."""
    image_id: Optional[str] = Field(default=None, description="ID of the generated image")
    """ID of the generated image"""
    download_url: str = Field(..., description="URL to download the image")
    """URL to download the image"""
    thumbnail_url: Optional[str] = Field(default=None, description="URL to view a thumbnail")
    """URL to view a thumbnail"""
    b64_data: Optional[str] = Field(default=None, description="Base64 encoded image data if applicable")
    """Base64 encoded image data if applicable"""
    metadata: Optional[ImageMetadata] = Field(default=None, description="Additional metadata related to the image generation")
    """Additional metadata related to the image generation"""

    class Config:
        extra = "ignore"