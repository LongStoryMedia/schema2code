# Code generated by schema2code at 2025-08-03 11:47:13. DO NOT EDIT.


from typing import List, Dict, Optional, Any, Union
from datetime import datetime, date, time, timedelta
from .message_role import MessageRole
from .message_content import MessageContent
from pydantic import BaseModel, Field, AnyUrl, EmailStr



"""Message represents a single exchange in the conversation"""
class Message(BaseModel):
    # Role of the message sender (e.g., user, assistant)
    role: MessageRole = Field(..., description="Role of the message sender (e.g., user, assistant)")
    # Content of the message
    content: List[MessageContent] = Field(..., description="Content of the message")
    # Optional tool calls associated with the message
    tool_calls: Optional[List[Dict[str, Any]]] = Field(None, description="Optional tool calls associated with the message")
    # Optional thinking process of the model, if applicable
    thinking: Optional[str] = Field(None, description="Optional thinking process of the model, if applicable")
    # Unique identifier for the message
    id: Optional[int] = Field(None, description="Unique identifier for the message")
    # Timestamp of when the message was created
    created_at: Optional[datetime] = Field(None, description="Timestamp of when the message was created")
    # Id of the conversation this message belongs to
    conversation_id: int = Field(..., description="Id of the conversation this message belongs to")

    class Config:
        extra = "ignore"